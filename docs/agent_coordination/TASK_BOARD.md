# LUB Agent Task Board

Use this file as the strict shared queue between Codex and Claude Code.

Rules:
- One implementation slice has one owner.
- Claude owns UI-first slices by default.
- Codex owns backend/data/runtime slices by default.
- Do not start a new slice until `In Progress` is updated.
- Move a task to `Blocked` instead of improvising across ownership boundaries.
- Commit hygiene is mandatory: stage only the slice manifest files (no `git add .` in dirty worktrees), verify with `git diff --cached --name-only`, then commit scoped changes only.

## Ready

| ID | Title | Owner | Scope | Files / Domains | Dependency / Blocker |
|----|-------|-------|-------|-----------------|----------------------|
| COD-MSME-SHOWCASE-001 | MSME product showcase platform (free + member tiers) | Codex + Claude | Build a product display platform for MSME businesses. Not a marketplace: businesses list products, visitors submit inquiries, and member status unlocks richer limits/features. | new DB tables, file storage for product images, public product listing/detail, inquiry flow, member product management dashboard, admin moderation, tier gating logic | Product refinement needed with user before starting. |
| COD-MSME-ISSUES-001 | MSME issue intake, AI categorization, and representation workflow | Codex | Create a member-led issue submission platform with document upload, extraction, AI rephrasing/classification/tagging, and admin review/reporting. | signup/onboarding, document upload + OCR extraction, issue submission flow, AI settings/key wiring, issue taxonomy, admin review/analytics/reporting | Product refinement still needed for moderation, access model, and privacy/retention policy. |
| COD-PUBLIC-001 | Implement remaining public News page content | Codex | Replace placeholder public `news` route with real content. Events half completed by `CLAUDE-EVENTS-ACTIVITIES-IA-IMPLEMENT-008`. News half remains blocked because no backend content source is defined yet. | `src/pages/News.tsx`, supporting content/data source, routing/runtime verification | No backend content source identified yet. |
| COD-MEMBERS-EXPORT-002 | Low-priority export UX follow-ups | Codex | Deferred export-quality enhancements only after higher-priority product work: optional `Select all extras` / `Clear extras`, optional CSV export, optional remember-last-selection behavior. | `src/pages/AdminRegistrations.tsx`, `src/lib/xlsxExport.ts`, export utility/runtime surface | Lowest priority by user direction. |

## In Progress

| ID | Title | Owner | Scope | Files / Domains | Dependency / Blocker |
|----|-------|-------|-------|-----------------|----------------------|
| COD-EVENTS-CMS-AI-AUTOFILL-038 | Event Brief AI autofill + slug UX overhaul + agenda public toggle | Claude | Single batch on top of 032. New migration `20260504000000_events_ai_autofill_and_slug_lock.sql` adds `events.show_agenda_publicly`, `events.slug_locked`, `events.ai_metadata`; new `check_event_slug_available_with_session` RPC; CREATE OR REPLACE rewrites of create/update/get-by-id/get-by-slug/get-published-events/get-all-events RPCs to honor slug_locked (rejects collisions with `error_code='slug_conflict'` instead of auto-suffixing), gate public agenda by `show_agenda_publicly`, and FIX the pre-existing pagination bug in `get_published_events` + `get_all_events_with_session` (slice rows BEFORE `jsonb_agg`). New Edge Function `draft-event-content` (cloned from `draft-activity-content`) reads `ai_runtime_settings` row `event_drafting`, supports draft + extract_fields modes with up to 3 source files (image ≤10MB / PDF ≤20MB / total ≤30MB) and a 4000-char brief; gated on `events.create` OR `events.edit_any` OR `events.edit_own`. Service `eventsService` gained `draftFromBrief` + `checkSlugAvailable` plus widened `create`/`update` returns to surface `error_code='slug_conflict'` + `conflict_slug`. `AdminEventForm` rewritten: prominent Event Brief panel with attach-(+) and Generate; loading/partial/success/failed AI states; slug renders as read-only chip with `Edit slug` toggle, debounced availability check, `Reset to auto`, save blocked on confirmed-taken/invalid slug; Show-agenda-publicly toggle near agenda; published-event Generate confirms before overwriting. ActivityDetail event branch already gates agenda on `agenda_items.length > 0` so the server-side gate is sufficient. No permission changes; no contract break elsewhere. | `supabase/migrations/20260504000000_events_ai_autofill_and_slug_lock.sql`, `supabase/functions/draft-event-content/index.ts`, `src/lib/supabase.ts`, `src/pages/AdminEventForm.tsx` | Local lint+build PASS, Phase 1 readonly smoke PASS (3/12). Pending Codex: apply migration, deploy `draft-event-content`, seed `ai_runtime_settings` row `event_drafting`, run live RPC + Edge Function probes (slug-availability happy/taken, slug_conflict on locked save, public agenda gating, paginated list verification, AI draft + extract_fields). |

## Blocked

| ID | Title | Owner | Scope | Files / Domains | Dependency / Blocker |
|----|-------|-------|-------|-----------------|----------------------|

## Completed Recently

| ID | Title | Owner | Scope | Files / Domains | Outcome |
|----|-------|-------|-------|-----------------|---------|
| COD-EVENTS-CMS-032 | Events CMS full implementation (schema + RPC + admin Events + unified public Events & Activities) | Codex | Completed end-to-end Events domain. Added dedicated events table, events.* permissions, secure _with_session CRUD/publish/archive/delete RPCs, public read RPCs with member-only visibility handling, service-layer eventsService, real admin Events list/form routes, and public /events integration that now reads both Events + Activities. /events/:slug now resolves Event first then Activity fallback. | supabase/migrations/20260503120000_events_cms_full.sql, src/lib/supabase.ts, src/pages/AdminEvents.tsx, src/pages/AdminEventForm.tsx, src/pages/Events.tsx, src/pages/ActivityDetail.tsx, src/components/admin/AppSidebar.tsx, src/App.tsx | Complete in repo. Validation PASS: lint (0 errors / 3 warnings), build PASS, Phase 1 readonly smoke PASS (3 passed / 12 skipped). |

| CLAUDE-SMART-UPLOAD-GST-CANDIDATES-017 | Smart Upload - Import button placement, GST Trade/Legal candidates, GST full_name fallback | Claude + Codex | Added `field_options` support for extraction results, moved `Import Data` below queued docs, surfaced Trade/Legal company-name candidate chips in `/join`, and added GST `full_name` fallback with Aadhaar precedence preserved. | `supabase/functions/extract-document/index.ts`, `src/lib/documentExtraction.ts`, `src/components/SmartUploadDocument.tsx`, `src/pages/Join.tsx` | Complete. Codex deployed `extract-document`; live invoke of `AA370425004153O_RC07042025.pdf` returned HTTP 200, `is_readable: true`, `detected_type: gst_certificate`, default Trade Name company value, GST `full_name`, and Trade+Legal options. Lint/build/Phase 1 readonly smoke PASS on 2026-04-26. |
| CLAUDE-SMART-UPLOAD-PDF-DATE-016 | Smart Upload deterministic GST PDF fallback + portal date display in review/conflict/summary | Claude + Codex | Added deterministic GST REG-06 text fallback in `extract-document` and formatted Smart Upload review/summary/conflict date displays via portal Date & Time settings while preserving canonical internal `YYYY-MM-DD` values. | `supabase/functions/extract-document/index.ts`, `src/components/SmartUploadDocument.tsx`, `src/pages/Join.tsx` | Complete. Codex deployed `extract-document`; live invoke of `AA370425004153O_RC07042025.pdf` returned HTTP 200, `is_readable: true`, `detected_type: gst_certificate`, and expected GST fields. Lint/build/Phase 1 readonly smoke PASS on 2026-04-26. |
| COD-ACTIVITIES-COVER-LIST-FALLBACK-015 | Activities list thumbnail effective cover fallback | Codex | Fixed admin/public list cover fallback by normalizing existing gallery-route cover seeds to cover-route seeds, updating list/detail RPCs to return the first gallery image as effective cover when no explicit cover exists, adding `first_media_url` to the admin list RPC, and hardening the admin list `Photo` thumbnail fallback. | `supabase/migrations/20260426090000_activity_effective_cover_fallback.sql`, `supabase/migrations/20260426093000_activity_admin_list_first_media_url.sql`, `src/lib/activityMedia.ts`, `src/lib/supabase.ts`, `src/pages/AdminActivityForm.tsx`, `src/pages/AdminActivities.tsx` | Complete. Migrations applied. Live RPC probe confirmed `cover-card` HTTP 200 image response. Lint/build/Phase 1 readonly smoke PASS on 2026-04-26. |
| COD-ACTIVITIES-SLUG-SEARCH-014 | Activities slug uniqueness + public Events smart search | Codex | Added server-side slug normalization/de-duplication for Activities create/update and implemented ranked smart search with All/Featured/Upcoming/Past filters on public `/events`. Admin slug UI now uses canonical `/events/` prefix and clearer published-slug wording. | `supabase/migrations/20260425150000_activity_slug_uniqueness_and_search.sql`, `src/pages/AdminActivityForm.tsx`, `src/pages/Events.tsx`, `src/lib/supabase.ts` | Complete. Migration applied. Lint/build/Phase 1 readonly smoke PASS on 2026-04-25. |
| COD-ACTIVITIES-MEDIA-AI-FOLLOWUP-013 | Activities gallery batch upload, gallery-cover fallback, and AI date/location extraction options | Codex | Fixed admin Activities gallery multi-select queue so all selected files continue through the crop modal; added save/publish fallback that uses the first active gallery image as cover when no cover is selected; extended source-document extraction to return first-day `activity_date` plus selectable date/location candidates and prefill empty main Activity Details fields. Redeployed `draft-activity-content`. | `src/pages/AdminActivityForm.tsx`, `src/lib/supabase.ts`, `supabase/functions/draft-activity-content/index.ts` | Complete. `supabase functions deploy draft-activity-content`, lint, build, and Phase 1 readonly smoke PASS on 2026-04-25. |
| COD-ACTIVITIES-CLOUDFLARE-MEDIA-012 | Move Activities media originals to private Cloudflare R2 + Worker-served variants | Codex | Implemented private-original Cloudflare media flow for Activities. Migration adds original-object metadata to `activities` / `activity_media`; new Supabase Edge Functions handle upload/delete/original-download; Worker `lub-media` serves display variants from private R2 bucket `lub` on `media.lub.org.in`; admin form supports Cloudflare uploads and `Download Original`. | `src/lib/supabase.ts`, `src/lib/activityMedia.ts`, `src/pages/AdminActivityForm.tsx`, `src/pages/AdminActivities.tsx`, `src/pages/Events.tsx`, `src/pages/ActivityDetail.tsx`, `src/components/ImageCropModal.tsx`, `supabase/functions/activity-media-*`, `supabase/functions/_shared/activity-media-shared.ts`, `supabase/migrations/20260420130000_activities_cloudflare_media_support.sql`, `cloudflare/lub-media/**`, `docs/cloudflare_events_media_setup.md`, `.env.example` | Complete. Migration applied, Worker deployed, functions deployed, live disposable probe passed for cover/gallery upload, Worker display variants, signed original downloads, public no-variant rejection, and cleanup. Lint/build/Phase 1 readonly smoke PASS on 2026-04-25. |
| COD-SMART-UPLOAD-GENDER-PRECEDENCE-011 | Smart Upload review/source cleanup + document-precedence gender override in Join flow | Codex | Codex separated raw Smart Upload review state from draft-only autofill state, added source labels showing whether document data matches or overrides current account/form values, and treated `gender` as a document-precedence field in `/join`. Final browser-verified mismatch case: signup gender `female` plus Aadhaar gender `male` shows `GENDER: Male` with `Document overrides current account/form value`, and continuing to the registration form sets `gender=male`. The override is intentionally scoped to the registration flow and does not mutate the separate account/signup record. | `src/components/SmartUploadDocument.tsx`, `src/pages/Join.tsx` | Complete and browser-verified by Codex on 2026-04-20. |
| CLAUDE-SMART-UPLOAD-GENDER-010 | Fix Aadhaar smart-upload gender handling on Extracted data review + strengthen backend extraction | Claude + Codex | Codex deployed `extract-document`, live-invoked the Aadhaar sample directly, and browser-verified `/join` end to end with a fresh member account. Final root cause was mixed: backend extraction needed the Aadhaar-specific gender hardening that Claude added, and the review card still used draft-only Smart Upload state, which drops successfully extracted fields when they match an existing form value. Codex fixed the remaining client bug by surfacing raw extracted fields from `SmartUploadDocument` into a separate review state in `Join.tsx`, so the review now shows `GENDER: Male` and no longer falsely renders `Unable to extract gender` when extraction succeeded. Final browser verification confirmed: Aadhaar review shows full name, DOB, and gender; the red warning is gone; continuing to the form preserves `gender=male`. | `supabase/functions/extract-document/index.ts`, `src/components/SmartUploadDocument.tsx`, `src/pages/Join.tsx` | Complete and browser-verified by Codex on 2026-04-20. |
| CLAUDE-ADMIN-ACCESS-REDIRECT-009 | Fix admin route infinite redirect loop for authenticated non-admin users | Claude + Codex | `AdminLayout` now distinguishes `unauthenticated` from `authenticated-unauthorized`, sending unauthenticated users to `/signin` and authenticated non-admin users to `/dashboard`. Codex live verification confirmed: admin still reaches `/admin/dashboard`, cleared session goes to `/signin`, and a fresh authenticated `general_user` is redirected from both `/admin/dashboard` and `/admin/members/registrations` to `/dashboard` with no loading hang or loop. | `src/components/admin/AdminLayout.tsx` | Complete and browser-verified by Codex on 2026-04-20. |
| COD-EVENTS-ACTIVITIES-IA-001 | Events/Activities public IA rework + public Events content | Claude + Codex | Claude implementation made `/events` the real public feed, added `/events/:slug`, converted `/activities` and `/activities/:slug` into compatibility redirects, removed public `Activities` nav/footer links, renamed the admin sidebar section to `Events & Activities`, and updated admin `View Public Page` to `/events/:slug`. Codex browser verification confirmed all route, nav, sidebar, and link behavior. | `src/App.tsx`, `src/pages/Events.tsx`, `src/pages/Activities.tsx`, `src/pages/ActivityDetail.tsx`, `src/components/Header.tsx`, `src/components/Footer.tsx`, `src/components/admin/AppSidebar.tsx`, `src/pages/AdminActivities.tsx` | Complete and browser-verified by Codex on 2026-04-20. |
| COD-ACTIVITIES-ACTIONS-MENU-005 | Fix admin Activities row Actions dropdown rendering off-screen | Claude + Codex | Claude correction pass replaced `DropdownMenuTrigger asChild + Button` with a direct trigger using `buttonVariants`, resolving the missing-ref anchor problem. Codex live browser verification confirmed wrapper box `x=1215 y=467 width=176 height=188` and all 5 items render visibly on-screen. | `src/pages/AdminActivities.tsx` | Complete and browser-verified by Codex on 2026-04-19. |
| COD-ACTIVITIES-AI-EXTRACT-BTN-004 | Separate source-document extraction from draft generation with an explicit `Extract Content` button | Claude + Codex | Verified complete. Uploading source documents no longer auto-triggers extraction. The explicit `Extract Content` button appears, extraction runs only when clicked, and guided fields prefill afterward while remaining editable before `Generate Draft`. | `src/pages/AdminActivityForm.tsx` | Complete and browser-verified by Codex on 2026-04-19. |
| COD-ACTIVITIES-HANDOFF-VERIFY-001 | Apply/deploy/runtime-verify Activities AI + list follow-up batches | Codex | Applied `20260420000000`, deployed `draft-activity-content`, and closed the remaining Activities runtime handoff. | `supabase/migrations/20260420000000_activities_limits_default_bump.sql`, `supabase/functions/draft-activity-content/index.ts` | Complete. All runtime checks passed. |
| CLAUDE-ACTIVITIES-NEXT-003 | Activities: source-doc extraction prefill + sort controls + ranked search | Claude | Added AI source-document extraction prefill with 30 MB total cap, sort controls plus explicit `Actions` header, and ranked Activities admin search with Featured-only filtering. | `src/pages/AdminActivities.tsx`, `src/pages/AdminActivityForm.tsx`, `src/lib/supabase.ts`, `supabase/functions/draft-activity-content/index.ts` | Complete. Runtime handoff now closed by `COD-ACTIVITIES-HANDOFF-VERIFY-001`. |
| CLAUDE-ACTIVITIES-FOLLOWUP-002 | Activities AI/provider/media follow-up | Claude | Added honest OpenAI-only AI gating, AI source-document support, structured `error_code` handling, and in-modal gallery ratio selection with portrait options. | `src/pages/AdminActivityForm.tsx`, `src/components/ImageCropModal.tsx`, `src/lib/supabase.ts`, `supabase/functions/draft-activity-content/index.ts` | Complete. Runtime handoff now closed. |
| CLAUDE-ACTIVITIES-BATCH-001 | Activities batch: dropdown fix, limits UI, media UX, AI draft helper, homepage CTA | Claude | Fixed Activities row actions dropdown attempt, added Activities settings UI and higher default gallery cap, improved media handling, added AI draft helper foundation, and changed homepage CTA to `Explore Activities`. | `src/pages/AdminActivities.tsx`, `src/pages/AdminActivityForm.tsx`, `src/pages/AdminActivitySettings.tsx`, `src/pages/Home.tsx`, `src/lib/supabase.ts`, `src/lib/imageProcessing.ts`, `supabase/functions/draft-activity-content/index.ts`, `supabase/migrations/20260420000000_activities_limits_default_bump.sql` | Complete. Live runtime closeout done by Codex. |





