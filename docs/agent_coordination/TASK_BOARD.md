# LUB Agent Task Board

Use this file as the strict shared queue between Codex and Claude Code.

Rules:
- One implementation slice has one owner.
- Claude owns UI-first slices by default.
- Codex owns backend/data/runtime slices by default.
- Do not start a new slice until `In Progress` is updated.
- Move a task to `Blocked` instead of improvising across ownership boundaries.

## Ready

| ID | Title | Owner | Scope | Files / Domains | Dependency / Blocker |
|----|-------|-------|-------|-----------------|----------------------|
| COD-MSME-SHOWCASE-001 | MSME product showcase platform (free + member tiers) | Codex + Claude | Build a product display platform for MSME businesses (manufacturers and similar). Not a marketplace — businesses list products with photos and descriptions; visitors submit inquiries. Two access tiers: (1) Free — basic listing, limited products/photos; (2) Member — unlocked on active paid membership, higher product/photo limits and richer display options. Specific tier limits and display options to be confirmed with user before implementation. Feature is higher priority than news/events. | new DB tables (products, inquiries, tier config), file storage for product images, public product listing page, product detail + inquiry form, member product management dashboard, admin moderation view, tier gating logic wired to membership status | Product refinement needed: exact per-tier limits (product count, photo count, video/brochure support), inquiry notification path (email/in-app), moderation/approval workflow, and whether non-member free-tier registration is separate from LUB member registration. Scope with user before starting. |
| COD-PUBLIC-001 | Implement public Events, News, and Activities page content | Codex | Investigate why the public `events`, `news`, and `activities` routes currently render only hero headings, then add the intended content/data rendering and verify the pages are populated | public site pages, any supporting content/data source, routing/runtime verification | Root cause unclear from screenshots; may require both content wiring and UI completion |
| COD-MSME-ISSUES-001 | MSME issue intake, AI categorization, and representation workflow | Codex | Create a member-led issue submission platform where MSME businesses can upload business proof documents, prefill registration details from extracted document data, submit operational/government issues, and have AI rephrase, classify, tag, and aggregate issues by industry, geography, and request type for LUB review and representation planning | signup/onboarding, document upload + OCR extraction, member issue submission flow, AI settings/key wiring, issue taxonomy, admin review/analytics/reporting, runtime verification | Product refinement still needed for moderation rules, allowed document types, member-vs-nonmember access, and privacy/data-retention policy |
| CLAUDE-DATETIME-FORMAT-001 | Add global admin date and time format settings | Claude | Add a settings feature in the admin section where date format and time format can be selected and applied globally across the website | admin settings UI, global date/time formatting preferences, shared display formatting surfaces, runtime verification | User requested Claude ownership; implementation should ensure the selected formats propagate consistently across admin and public/member-facing displays |
| COD-MEMBERS-EXPORT-002 | Low-priority export UX follow-ups | Codex | Deferred export-quality enhancements only after higher-priority product work: optional `Select all extras` / `Clear extras` controls in the export dialog, optional CSV export alongside XLSX, and optional remember-last-selection behavior for each admin. Existing approved-only XLSX export v2 is already complete and should remain unchanged until this slice is explicitly started. | `src/pages/AdminRegistrations.tsx`, `src/lib/xlsxExport.ts`, export utility/runtime surface | Lowest priority by user direction. Do not start until showcase/public/issues/date-format work is handled or user explicitly re-prioritizes it. |

## In Progress

| ID | Title | Owner | Scope | Files / Domains | Dependency / Blocker |
|----|-------|-------|-------|-----------------|----------------------|
| None | - | - | - | - | - |

## Blocked

| ID | Title | Owner | Scope | Files / Domains | Dependency / Blocker |
|----|-------|-------|-------|-----------------|----------------------|
| None | - | - | - | - | - |

## Done This Week

| ID | Title | Owner | Scope | Files / Domains | Dependency / Blocker |
|----|-------|-------|-------|-----------------|----------------------|
| CLAUDE-USR-MODAL-ZINDEX-001 | Fix Block Account and Assign Role modal z-index in Admin Users | Claude | Fixed z-index stacking bug causing Block Account and Assign Role dialogs to render behind the overlay backdrop in Admin Users (`/admin/administration/users`). Root cause: both modals used `z-50` (same level as Radix dropdown portal) and had no `relative z-10` on the card. Applied the established fixed-modal pattern to both files: outer shell `z-50` → `z-[80]`, added `relative z-10` to dialog card, updated centering wrapper, removed legacy zero-width spacer. No backend or RPC changes. | `src/components/admin/modals/BlockUserModal.tsx`, `src/components/admin/modals/AssignRoleModal.tsx` | Complete. `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped). |
| COD-MEMBERS-EXPORT-001 | Add admin approved-members XLSX export | Codex | Completed approved-members XLSX export v2 on the existing admin registrations page (`/admin/members/registrations`) without creating a new members page. Initial migration `20260416093000_add_admin_members_export_rpc.sql` added session-wrapped RPC `get_admin_approved_members_export_with_session`, and follow-up migration `20260416113000_extend_admin_members_export_rpc_optional_fields.sql` extended the same approved-only contract with optional export fields (`mobile_number`, `email`, `member_id`, `company_address`, `gender`). `src/lib/supabase.ts` now exposes the richer `ApprovedMemberExportRow` and `memberRegistrationService.getApprovedMembersExport()`. `src/lib/xlsxExport.ts` remains the XLSX generator. `AdminRegistrations.tsx` now opens an export dialog with fixed core columns (`Company Name`, `Member Name`, `City`, `District`) plus optional checkbox fields (`Mobile`, `Email`, `Membership ID`, `Address`, `Gender`); core columns stay selected by default, extras start unchecked, and the export remains independent of current client-side filters while always exporting approved members only. | `supabase/migrations/20260416093000_add_admin_members_export_rpc.sql`, `supabase/migrations/20260416113000_extend_admin_members_export_rpc_optional_fields.sql`, `src/lib/supabase.ts`, `src/lib/xlsxExport.ts`, `src/pages/AdminRegistrations.tsx` | Complete. `npm run db:migrations:audit` PASS, `npm run db:migration:apply:single -- --version=20260416113000` PASS, `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped). Targeted browser export-dialog verification PASS: dialog defaults correct, pending page filter did not affect approved-only export scope, core-only workbook headers matched exactly, and selected extra headers were added correctly in `LUB_Members_2026-04-16.xlsx`. |
| CLAUDE-NORMALIZATION-RULES-UI-001 | Add admin Normalization Rules settings page and wiring | Claude | New page `src/pages/AdminNormalizationSettings.tsx` at `/admin/settings/normalization`: rules grouped by category (identity/contact/company/business/referral), per-row enable/disable toggle, inline instruction-text edit with Save/Cancel, restore-default action using `default_instruction_text` (no hardcoded duplication), reorder up/down, "customised" badge when instruction differs from default, audit trail only when `updated_by_email` set. Scope info banner clearly distinguishes Verify-time AI cleanup from regex validation and Smart Upload extraction. PermissionGate on `settings.normalization.view` with read-only message when user lacks manage. Settings Hub Normalization Rules card added (Wand2 icon, `settings.normalization.view` permission). Sidebar entry added. Route wired. No changes to `src/lib/supabase.ts`, `normalize-member`, or Verify flow. | `src/pages/AdminNormalizationSettings.tsx` (new), `src/pages/AdminSettingsHub.tsx`, `src/components/admin/AppSidebar.tsx`, `src/App.tsx` | Complete. `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped). |
| NORMALIZATION-RULES-ADMIN-001 | Admin-configurable Verify-time normalization backend/runtime | Codex | Added a dedicated normalization settings backend domain for Member Registration Verify-time cleanup without changing the Verify/Submit flow. New migration `20260415143000_add_normalization_rules_admin_contracts.sql` creates `member_normalization_rules`, seeds all 9 normalization field rows, adds `settings.normalization.view/manage`, and exports session-wrapped read/update/reorder RPCs. `src/lib/supabase.ts` now exports `NormalizationRule`, `NormalizationRuleCategory`, and `normalizationRulesService`. `supabase/functions/normalize-member/index.ts` now loads normalization rules from DB in parallel with AI runtime settings, falls back to the previous hardcoded defaults when DB rules are unavailable, and short-circuits to passthrough when all effective rules are disabled. Verify flow contract in Join remains unchanged. | `supabase/migrations/20260415143000_add_normalization_rules_admin_contracts.sql`, `src/lib/supabase.ts`, `supabase/functions/normalize-member/index.ts`, coordination docs | Complete. Migration applied safely (`--version=20260415143000`) after avoiding collision with an older remote `normalization_rules` table by using `member_normalization_rules`. `supabase functions deploy normalize-member` PASS. `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped). Direct function probe PASS with normalized output.
| SMART-UPLOAD-DOC-QUALITY-006 | Parser-first UDYAM PDF extraction + Aadhaar gender hardening | Codex | Replaced fragile PDF-only heuristics with parser-first extraction in `extract-document`: added `pdfjs-dist` text extraction with legacy fallback, tightened pdf-text -> pdf-vision retry behavior, added Responses JSON-mode enforcement for PDF vision, fixed PAN-anchor misclassification so UDYAM docs are not collapsed into PAN-only docs, expanded generic/document-specific key normalization and prompt rules for UDYAM/Aadhaar, and added second-pass doc-specific enrichment for auto-detected uploads. Live verification with the real UDYAM sample now extracts company/address/business fields correctly; live verification with a real Aadhaar image now extracts `full_name`, `date_of_birth`, and `gender`. | `supabase/functions/extract-document/index.ts`, coordination docs, live deploy/probes | Complete. `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped on final run), `supabase functions deploy extract-document` PASS. Live probes PASS for real UDYAM PDF and Aadhaar image samples. |
| CLAUDE-SMART-ENTRY-UI-002 | Refine Smart Upload entry-step layout and document selector UX | Claude | Reordered `SMART_UPLOAD_REQUIRED_DOC_OPTIONS` alphabetically (Aadhaar Card, GST Certificate, PAN Card (Company), Payment Proof, UDYAM Certificate) with Others appended last. Removed `smartUploadGuide` computed value. Removed the Recommended Documents panel and the two-column grid wrapper. Smart stage is now single-column: heading → SmartUploadDocument → Extracted Data Review (full-width) → button row. Removed "Fill Manually Instead" button. Back now navigates with `window.history.length > 1 ? navigate(-1) : navigate('/')`. `SmartUploadDocument.tsx` unchanged. | `src/pages/Join.tsx` | Complete. `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped). |
| COD-SMART-BATCH-005 | Smart Upload batch: case-insensitive matching, GST autofill, and pre-registration entry flow | Codex | Completed the bundled Smart Upload improvement slice: conflict comparison is now case-insensitive only for option-like fields while extracted location values are normalized to DB-backed casing before comparison/autofill; GST certificate imports now sanitize GST number, set `gst_registered = yes`, and derive PAN from GSTIN when PAN is absent; new authenticated users without an existing registration now see a pre-registration choice screen for Smart Upload-assisted registration vs manual entry, with staged extracted-data review before the form. Registration-status lookup failures now block with retry instead of exposing a duplicate-registration path. Secondary review pass found no remaining material logic issues after fixes. | `src/components/SmartUploadDocument.tsx`, `src/pages/Join.tsx`, coordination docs | Complete. `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS. `npm run test:e2e:phase1:local` was unstable in this environment with unrelated admin auth/permission denials on `/admin/dashboard`; Join/Smart Upload slice itself was code-reviewed twice and the follow-up review found no remaining material issues. |
| CLAUDE-SMART-UPLOAD-COPY-001 + CLAUDE-FORM-UPLOAD-002 + CLAUDE-AVATAR-FALLBACK-001 | Smart Upload copy update, file-input audit, header avatar fallback | Claude | (1) COPY-001: Changed Smart Upload helper text from `Upload documents` to `Upload all documents` in `SmartUploadDocument.tsx`. (2) FORM-UPLOAD-002: Full audit of all `type="file"` inputs — all are already using `hidden`/`sr-only` + styled label buttons from prior CLAUDE-FORM-UPLOAD-001 slice; confirmed no-op. (3) AVATAR-FALLBACK-001: Removed `getInitials`/`getAvatarColor` helper functions from `Header.tsx`. Added `photoError` state + reset `useEffect`. Desktop and mobile avatar now render a `User` icon in a `bg-muted` circle for (a) members with broken/missing profile photo and (b) auth-only users (`member = null`). Auth-only users see a "Signed in / Registration incomplete" dropdown with a "Complete Registration" link to `/join` and a Logout button, instead of the confusing "Sign In" button. | `src/components/SmartUploadDocument.tsx` (copy edit), `src/components/Header.tsx` (avatar + auth-only branch) | Complete. `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped). |
| COD-SMART-PAYMENT-READ-001 + COD-USER-PROFILE-GATE-001 | Smart-upload payment-field guard + non-registered user edit-profile gate | Codex | Closed two backend/runtime slices in one pass: (1) hardened extract-document doc-type resolution so payment proofs cannot leak payee-org/company data into business fields via misclassification; (2) gated Member Edit for authenticated users with no registration submission while prefilling from available signup/session/user data and blocking edit/verify/submit until first registration exists. | `supabase/functions/extract-document/index.ts`, `src/pages/MemberEditProfile.tsx`, coordination docs | Complete. `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped). Claude `CLAUDE-AVATAR-FALLBACK-001` unblocked with contract: `useMember` shape unchanged; auth-only users remain `member=null`. |
| COD-SIGNUP-PREFILL-001 | Join prefill hydration from signup payload + session user data | Codex | Added hardened session RPC contract to fetch latest Signup V2 submission payload (core + custom) and wired Join prefill to hydrate all supported member-registration fields from existing sources in priority order (existing registration, signup payload, user/session profile) without overwriting already-entered values. Also ensured district-id hydration when district is prefilled so city chain loads correctly. | `supabase/migrations/20260413164500_add_signup_prefill_payload_session_rpc.sql`, `src/lib/supabase.ts`, `src/pages/Join.tsx` | Complete. `npm run db:migrations:audit` PASS, targeted migration apply PASS (`--version=20260413164500`), `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped). |
| CLAUDE-SMART-UPLOAD-BATCH-002 | Smart Upload UX rework: remove type dropdown, add Import Data button, PDF vision fallback | Claude | Removed manual doc-type selection from Smart Upload UI entirely. Files queue on upload; processing only starts on Import Data click. Added `runPdfVisionPipeline` in edge function to handle scanned/image-based PDFs via OpenAI `input_file` (previously returned `unsupported_format`). Updated `SmartUploadItem` to track only AI-detected type. Merge priority rules and conflict modal unchanged. | `supabase/functions/extract-document/index.ts`, `src/components/SmartUploadDocument.tsx`, `src/lib/documentExtraction.ts` | Complete. `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3/12), `supabase functions deploy extract-document` PASS. |
| COD-SMART-DOC-PRIORITY-004 | Smart Upload extraction policy v2 (document-specific fields + priority-based prefill) | Codex | Enforced strict extraction ownership and merge precedence for Smart Upload: Aadhaar-only identity fields, PAN-only PAN field, payment-proof-only payment fields, and GST/Udyam company-location-business ownership with deterministic priorities. Added per-field source tracking in UI so lower-priority document extracts cannot overwrite higher-priority Smart Upload values. Expanded business autofill support in Join for `industry`, `activity_type`, and `products_services`. | `supabase/functions/extract-document/index.ts`, `src/components/SmartUploadDocument.tsx`, `src/pages/Join.tsx` | Complete. `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped), and `supabase functions deploy extract-document` PASS. |
| CLAUDE-FIELD-LENGTH-001 | Implement field-level min/max length frontend consumption | Claude | Added `min_length`/`max_length` to `useFormFieldConfig` hook (FieldConfigMap, all 4 builder branches, `getFieldMinLength`/`getFieldMaxLength` helpers). Added Min/Max Length number inputs to `AdminFieldLibrary` create/edit form (applicable types: text/textarea/email/tel/number/url). `EMPTY_FORM` + `startEdit` pre-fill updated. `getSubmitValue()` strips length for non-applicable types. `maxLength` HTML attr added to all editable text/textarea inputs in Join + MemberEditProfile. `validateForm` in both forms now checks min/max length before regex. | `src/hooks/useFormFieldConfig.ts`, `src/pages/AdminFieldLibrary.tsx`, `src/pages/Join.tsx`, `src/pages/MemberEditProfile.tsx` | Complete. `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped). |
| COD-FIELD-LENGTH-001 | Add min/max length contracts for Form Builder fields | Codex | Added nullable `min_length` / `max_length` to Form Builder V2 field library + draft/live form-field tables, extended read contracts (Join/MemberEdit/Signup/Signin live+draft + builder/library schema payloads), extended field-library write contracts with type-aware length guards, and exported service/type support in `src/lib/supabase.ts` for frontend consumption | `supabase/migrations/20260412235500_add_field_length_contracts_v2.sql`, `src/lib/supabase.ts` | Complete. Targeted migration applied safely (`--version=20260412235500`). `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped). Unblocks Claude `CLAUDE-FIELD-LENGTH-001`. |
| COD-AI-RUNTIME-003 | Move normalize-member runtime key source from Edge secret to AI Settings DB | Codex | Implemented and deployed new `normalize-member` Edge function (v11) that reads provider/model/reasoning/api key from `public.ai_runtime_settings` (`member_normalization`) using service-role DB access, replacing direct `OPENAI_API_KEY` secret dependency; verified runtime normalization success and deployed-bundle secret reference removal | `supabase/functions/normalize-member/index.ts`, live deploy (`supabase functions deploy normalize-member`), runtime probe via `/functions/v1/normalize-member`, management API bundle scan | Complete. Verification PASS: response normalization works with DB key; remote bundle scan shows `OPENAI_API_KEY=MISS` for both `normalize-member` and `send-email`; safe to delete `OPENAI_API_KEY` from Edge secrets now. |
| COD-AI-VERIFY-003 | Verify whether `normalize-member` still requires Edge secret `OPENAI_API_KEY` | Codex | Completed read-only live verification of deployed `normalize-member` bundle dependency chain to determine safe deletion state for Edge secret `OPENAI_API_KEY` | Supabase CLI function list + management API function-body pull (`/functions/normalize-member/body`), binary string scan evidence | Complete. Found deployed bundle explicitly contains `const OPENAI_API_KEY = Deno.env.get("OPENAI_API_KEY")` and error string `OPENAI_API_KEY not configured`; safe decision: **do not delete secret yet**. |
| COD-AI-SETTINGS-002 | Add OpenAI reasoning dropdown in AI Settings | Codex | Added persisted OpenAI reasoning-effort setting (`low`, `medium`, `high`, `xhigh`) end-to-end: DB column + constraints, updated AI settings RPC contracts, service type/payload mapping, runtime profile export, and conditional admin dropdown shown only when provider is OpenAI. Existing `_with_session` permission model preserved. | `supabase/migrations/20260412222000_add_ai_runtime_reasoning_effort.sql`, `src/lib/supabase.ts`, `src/pages/AdminAISettings.tsx` | Complete. `db:migrations:audit` PASS, `db:migration:apply:single -- --version=20260412222000` PASS, `npm run lint` PASS (0 errors / 3 expected warnings; first run transient ENOENT, rerun clean), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped). |
| COD-VAL-DESC-002 | Persist validation-rule description edits end-to-end | Codex | Completed backend persistence contract for validation-rule description edits added by Claude UI: extended `update_validation_rule_with_session` RPC to accept `p_description`, updated `validationRulesService.updateValidationRule` type and RPC payload, and wired `editedDescription` into save path so description updates survive reloads | `supabase/migrations/20260412201500_update_validation_rule_with_description_param.sql`, `src/lib/supabase.ts`, `src/pages/AdminValidationSettings.tsx` | Complete. `db:migrations:audit` PASS, `db:migration:apply:single -- --version=20260412201500` PASS, `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped). |
| CLAUDE-THEME-001 + CLAUDE-SIDEBAR-LOGO-001 + CLAUDE-VAL-DESC-001 | Theme rename, logo collapse trigger, validation description edit | Claude | (1) Renamed user-facing "Appearance" → "Theme" in sidebar nav, Settings Hub card, and Appearance settings page title. Route/path unchanged. (2) Wired sidebar logo button as collapse trigger: `toggleSidebar` on click, tooltip "Toggle navigation", `PanelLeftClose` hover affordance. Desktop `SidebarTrigger` hidden (`md:hidden`); mobile trigger preserved. (3) Added `editedDescription` state to Validation Settings edit form: textarea below error message, pre-filled on edit open, cleared on cancel. Note: description UI is complete; backend RPC `update_validation_rule_with_session` does not currently accept `p_description` — description changes will not persist until Codex extends the RPC. | `src/components/admin/AppSidebar.tsx`, `src/components/admin/AdminLayout.tsx`, `src/pages/AdminSettingsHub.tsx`, `src/pages/admin/AdminAppearanceSettings.tsx`, `src/pages/AdminValidationSettings.tsx` | Complete. `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped). |
| COD-AI-SETTINGS-001 | Add admin AI settings for API keys and model configuration | Codex | Implemented secure AI runtime settings foundation for normalization workflows: added DB-backed singleton settings table, new permissions (`settings.ai.view/manage`), session-protected read/write RPC wrappers with masked-key reads (no raw key return), public non-sensitive runtime profile contract, admin page/route/sidebar/settings-hub wiring, and runtime profile hint injection into normalization edge-function payload. | `supabase/migrations/20260412170000_add_ai_runtime_settings_admin_contracts.sql`, `src/lib/supabase.ts`, `src/lib/normalization.ts`, `src/pages/AdminAISettings.tsx`, `src/pages/AdminSettingsHub.tsx`, `src/components/admin/AppSidebar.tsx`, `src/App.tsx` | Complete. `db:migrations:audit` PASS, `db:migration:apply:single -- --version=20260412170000` PASS, `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped). |
| COD-APPEAR-LINK-002 | Token-link remaining non-tokenized classes (Join, admin, login/auth) | Codex | Completed non-tokenized class cleanup across targeted Join/admin/login-auth surfaces by replacing hardcoded color/shadow classes with appearance-tokenized classes (`border-border`, `bg-background/80`, `shadow-sm`, semantic token badges), including Form Studio/Form Editor preview/status surfaces; completed scoped hardcoded-class audit for the target domain with clean result (`NO_HARDCODED_CLASSES_FOUND`). | `src/pages/AdminDashboard/PaymentSettings.tsx`, `src/pages/AdminCityManagement.tsx`, `src/pages/AdminDashboard.tsx`, `src/pages/AdminDeletedMembers.tsx`, `src/pages/AdminDesignationsManagement.tsx`, `src/pages/AdminFormsList.tsx`, `src/pages/AdminLocationManagement.tsx`, `src/pages/AdminPendingCities.tsx`, `src/pages/AdminStateManagement.tsx`, `src/pages/AdminValidationSettings.tsx`, `src/pages/admin/AdminAppearanceSettings.tsx`, `src/pages/AdminFormStudio.tsx`, `src/pages/AdminFormEditorV2.tsx` | Complete. `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped). |
| COD-USR-001 | Fix user edit persistence and refresh Edit User modal styling | Codex | Root cause was modal layering, not RPC persistence: Edit User dialog sat under overlay making interaction unreliable. Updated modal to use the same stable layering pattern as Delete User (`z-[80]` shell, backdrop below, modal panel `z-10`, click isolation), plus tokenized success/error visuals for theme consistency. | `src/components/admin/modals/EditUserModal.tsx` | Complete. User confirmed working. `npm run lint` PASS (0 errors / 3 expected warnings). `npm run build` PASS. |
| CLAUDE-VERIFY-UX-001 + CLAUDE-VERIFY-UX-002 + CLAUDE-FORM-UPLOAD-001 | Verify spinner timing, auto-scroll to error, upload CTA wording | Claude | (1) Moved `setIsVerifying(true)` before `validateForm()` in both Join + MemberEditProfile so spinner activates immediately on Verify click. (2) Added `latestErrorsRef` + `scrollToFirstError` helper: on Verify validation failure, auto-scrolls/focuses first invalid DOM element by id; fallback to next valid key if element missing/hidden. (3) Standardized document upload CTAs: hidden native input + styled label-button showing `Upload File` (empty) or `Upload New File` (file selected or URL exists); `View current ...` links and file-name display preserved; profile photo copy unchanged. | `src/pages/Join.tsx`, `src/pages/MemberEditProfile.tsx` | Complete. `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped). |
| COD-VERIFY-BUG-001 + COD-VAL-LIVE-002 | Verify gating + live validation hardening (Member Edit + Member Registration) | Codex | Fixed Member Edit false-verified submit gate by making required checks config-driven for all visible+required fields (including `alternate_contact_name`) and added safe `onBlur` live validation hooks in both Member Edit and Member Registration so mapped rules (e.g. name format) show inline before final Verify | `src/pages/MemberEditProfile.tsx`, `src/pages/Join.tsx` | Complete. `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped). |
| COD-FORMS-PORTAL-009 | Dynamic field labels/placeholders for Member Registration + Member Edit runtime | Codex | Replaced hardcoded runtime field names/placeholders in `/join` and `/dashboard/edit` with Builder/Field Library metadata getters and dynamic select option mapping (safe fallbacks retained), while preserving visibility/required/validation/submission behavior | `src/hooks/useFormFieldConfig.ts`, `src/pages/Join.tsx`, `src/pages/MemberEditProfile.tsx` | Complete. `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped). |
| COD-FORMS-PORTAL-008 | Member Registration naming consistency pass (user-facing only) | Codex | Replaced remaining user-facing "Join LUB" wording with "Member Registration" in Join page heading, public home CTA, and Form Studio/Form Builder form-name display for `join_lub`, while preserving all technical keys/routes/contracts. | `src/pages/Join.tsx`, `src/pages/Home.tsx`, `src/pages/AdminFormStudio.tsx`, `src/pages/AdminFormBuilderV2.tsx` | Complete. `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped). |
| COD-FORMS-PORTAL-007 | Member Edit preview UX correction (back target + neutral heading style) | Codex | In preview mode (`/dashboard/edit?preview=1`), changed back/cancel navigation to return to Form Studio (not live profile) and removed blue-tinted heading strip so page header styling aligns with other public forms (signup/signin/member registration) | `src/pages/MemberEditProfile.tsx` | Complete. `npm run lint` PASS (0 errors / 3 expected warnings, first run had transient ENOENT lint artifact), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped). |
| COD-FORMS-PORTAL-006 | Prevent Member Edit preview from loading live user/member data | Codex | Fixed draft preview data isolation for `/dashboard/edit?preview=1`: preview mode no longer fetches live member profile data, resets form state to safe empty values, and avoids non-preview auth/profile guards so admins can inspect structure without exposing personal data | `src/pages/MemberEditProfile.tsx` | Complete. `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped on retry; first run hit known login-route flake). |
| COD-FORMS-PORTAL-005 | Fix remaining non-tokenized banner/header styles in Member Edit + tokenize Join action text states | Codex | Removed leftover hardcoded gradient/amber/white classes in Member Edit preview/header shell and converted Join submit/verify static white-text states to semantic token classes so both forms align better with Appearance settings | `src/pages/MemberEditProfile.tsx`, `src/pages/Join.tsx` | Complete. `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped). |
| COD-FORMS-PORTAL-004 | Theme-token parity hardening for Member Registration + Member Edit forms | Codex | Removed remaining hardcoded red/blue/green/shadow utility classes from Join + Member Edit form surfaces and replaced with appearance-tokenized classes so both flows follow active theme settings consistently | `src/pages/Join.tsx`, `src/pages/MemberEditProfile.tsx` | Complete. `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped). |
| COD-MEMBER-EDIT-PARITY-002 | Member Edit parity with Member Registration fields + data-preserving save | Codex | Brought Member Edit in line with registration coverage by adding missing document fields (`gst_certificate_url`, `udyam_certificate_url`, `payment_proof_url`) to load/edit/save flow, kept email/mobile lock-change UX intact, and hardened save payload to merge loaded data with explicit edits so unchanged values are preserved | `src/pages/MemberEditProfile.tsx`, `supabase/migrations/20260412103000_update_member_profile_include_document_urls.sql` | Complete. Targeted migration applied (`--version=20260412103000`). `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped). |
| COD-JOIN-RUNTIME-003 | Member Registration runtime parity + normalization verification | Codex | Closed remaining Join runtime parity gaps by making validation/submission/normalization field-aware to Builder live visibility + conditional applicability (notably GST-dependent inputs), and preventing hidden/inactive field values from being validated/submitted | `src/pages/Join.tsx` | Complete. `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped). |
| COD-TECH-002 | Fix build warnings (CSS minify selector + admin chunk size) | Codex | Removed malformed Tailwind selector generation in shared Card header class and refined Rollup manual chunk splitting so production build no longer emits css-syntax-error or >500k app-admin warning | `src/components/ui/card.tsx`, `vite.config.ts` | Complete. `npm run lint` PASS (0 errors / 3 expected warnings). `npm run build` PASS with no css-syntax/chunk-size warnings. `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped). |
| COD-USR-DELMODAL-002 | Fix Admin Users delete modal overlay/checkbox interaction | Codex | Corrected Delete User Account modal layering so the dialog reliably renders above the backdrop; checkbox interaction no longer closes the modal; updated warning/success/error blocks and CTA styles to tokenized destructive/success variants for clear visibility | `src/components/admin/modals/DeleteUserModal.tsx` | Complete. Lint PASS (0 errors / 3 expected warnings), build PASS, Phase1 readonly smoke PASS (3 passed / 12 skipped). |
| COD-REG-DEL-003 | Fix Member Registrations delete action no-op | Codex | Restored working delete flow in Admin Registrations Actions menu by adding the missing `openDeleteDialog` handler so Delete opens confirmation dialog and proceeds to the existing soft-delete/archive path | `src/pages/AdminRegistrations.tsx` | Complete. Lint PASS (0 errors / 3 expected warnings), build PASS, Phase1 readonly smoke PASS (3 passed / 12 skipped). |
| COD-REG-UX-002 | Admin Registrations + Edit Member modal bugfix bundle | Codex | Fixed pending-registration visual/count behavior and Edit Member modal defects: pending names emphasized, sidebar pending approvals count sourced via session RPC, custom-city required validation parity corrected, custom city value preserved when toggling city <-> Other, and close/success return flow now returns to the true origin (list vs application-review modal) | `src/pages/AdminRegistrations.tsx`, `src/components/EditMemberModal.tsx`, `src/contexts/AdminContext.tsx` | Complete. Lint PASS (0 errors / 3 expected warnings), build PASS, Phase1 readonly smoke PASS (3 passed / 12 skipped). |
| COD-FORM-SYSTEM-CLEAN-001 | Hide/remove system metadata fields from Builder user surfaces | Codex | Applied migration `20260410183000` to remove `id/status/created_at/updated_at` from builder schema, field-library listings, and join/member-edit runtime contracts; added backend guardrails so reserved keys cannot be attached/created again | `supabase/migrations/20260410183000_hide_system_fields_from_builder_access.sql` | Complete. `db:migrations:audit` PASS, `db:migration:apply:single -- --version=20260410183000` PASS, lint PASS (0 errors / 3 expected warnings), build PASS, Phase1 readonly smoke PASS (3 passed / 12 skipped). |
| COD-VAL-MEMBER-EDIT-001 | Form-aware validation mapping for Member Edit runtime | Codex | Fixed validation lookup to use form-specific Builder mapping (`member_edit` vs `join_lub`) and aligned Member Edit required/format validation with visibility rules so hidden fields do not block saves | `src/lib/validation.ts`, `src/hooks/useValidation.ts`, `src/pages/MemberEditProfile.tsx`, `src/pages/Join.tsx` | Complete. Lint PASS (0 errors / 3 expected warnings). Build PASS. Phase1 readonly smoke PASS (3 passed / 12 skipped). |
| COD-JOIN-PREVIEW-003 | Add admin-gated Join draft preview runtime contract | Codex | Added Join draft `_with_session` read RPC + client service draft method + runtime preview gate handling (`no_session` sign-in redirect, admin-only access block, no live fallback in preview mode); disabled preview verify/submit writes and updated smoke marker expectations for renamed heading copy | `supabase/migrations/20260410133000_add_join_form_draft_preview_contract.sql`, `src/lib/supabase.ts`, `src/hooks/useFormFieldConfig.ts`, `src/pages/Join.tsx`, `tests/e2e/phase1-production-smoke.spec.ts` | Complete. Migration applied via single-version push (`--version=20260410133000`). Lint PASS (0 errors / 3 expected warnings). Build PASS. Phase1 readonly smoke PASS (3 passed / 12 skipped). |
| COD-MEMBER-EDIT-BE-001 | Member Edit Form Builder backend/runtime contracts | Codex | Seeded `member_edit` form in Builder domain from `join_lub` baseline and shipped live/draft session RPC contracts plus typed client service exports to unblock pilot member-edit UI work | `supabase/migrations/20260410150000_add_member_edit_form_builder_runtime_contracts.sql`, `src/lib/supabase.ts`, `src/hooks/useFormFieldConfig.ts` | Complete. `db:migrations:audit` PASS, `db:migration:apply:single -- --version=20260410150000` PASS, lint PASS (0 errors / 3 expected warnings), build PASS, Phase1 readonly smoke PASS (3 passed / 12 skipped). |
| COD-SIGNUP-LEGACY-REMOVE-003 | Permanently remove legacy Signup fallback route/page | Codex | Hard removed `/signup-legacy` path and legacy route-switch env fallback so Signup remains single-path on Builder-driven V2 runtime only | `src/App.tsx`, `src/pages/SignUp.tsx`, `.env.example` | Complete. Lint PASS (0 errors / 3 expected warnings). Build PASS. Phase1 readonly smoke PASS (3 passed / 12 skipped). |
| COD-FORM-DEPRECATE-002 | Hard remove legacy Signup Form Configuration page | Codex | Removed obsolete admin Signup configuration route/page (`/admin/settings/forms/signup`) after confirming Form Builder is the single management path | `src/App.tsx`, `src/pages/AdminSignupFormConfiguration.tsx`, `src/pages/AdminFormsList.tsx` | Complete. Lint PASS (0 errors / 3 expected warnings). Build PASS. |
| COD-UI-013 | Remove Signup legacy card from Form Configuration hub | Codex | Removed the standalone Signup Form card from `/admin/settings/forms` so admins use centralized Form Builder as primary path; kept direct `/admin/settings/forms/signup` route intact for safe fallback during transition | `src/pages/AdminFormsList.tsx` | Complete. Lint PASS (0 errors / 3 expected warnings). Build PASS. |
| COD-JOIN-FORM-002 | Bring Join runtime under Builder live configuration | Codex | Added Join live runtime read RPC and frontend linkage so Join field visibility/required behavior now reads published Builder live schema (`join_lub`), with validation lookup aligned to Builder live mappings and legacy fallback retained for safety | `supabase/migrations/20260410004500_add_join_form_builder_runtime_read_contract.sql`, `src/lib/supabase.ts`, `src/hooks/useFormFieldConfig.ts`, `src/lib/validation.ts`, `src/pages/Join.tsx` | Complete. `db:migrations:audit` PASS, `db:migration:apply:single -- --version=20260410004500` PASS, lint PASS (0 errors / 3 expected warnings), build PASS, Phase1 readonly smoke PASS (3 passed / 12 skipped). |
| COD-SIGNIN-GUARD-002 | Prevent authenticated users from seeing `/signin` and return them to their prior page | Codex | Fixed `/signin` guard so logged-in users are redirected immediately (no sign-in form flash) to safe `next`, last non-signin route, same-origin referrer, then dashboard fallback. Added route-history tracker for non-signin routes. | `src/pages/SignIn.tsx`, `src/App.tsx` | Complete. Lint PASS (0 errors / 3 expected warnings). Build PASS. |
| CLAUDE-JOIN-EDIT-UI-001 | Member Edit pilot UI — Builder contract wiring + preview gate | Claude | (1) Member Edit: switched `useFormFieldConfig` from legacy → `builder_live`/`builder_draft` (formKey=`member_edit`). Added `isFieldVisible` guards to all configurable fields (personal/company/business/registration/alternate sections). Added preview gate (`no_session`→redirect, `access_denied`/`load_failed`→block screen), draft preview banner, submit+verify disabled in preview mode. (2) Studio: extended preview path ternary with `member_edit`→`/dashboard/edit?preview=1`. | `src/pages/MemberEditProfile.tsx`, `src/pages/AdminFormStudio.tsx` | Complete. Lint PASS (0 errors / 3 expected warnings). Build PASS. Phase1 readonly smoke PASS (3 passed / 12 skipped). |
| CLAUDE-TERM-001 | Rename "Join LUB Form" → "Member Registration Form" in UI labels | Claude | Renamed display heading in Form Configuration hub card and Field Configuration page title. Technical keys, form_key values, and route paths unchanged. | `src/pages/AdminFormsList.tsx`, `src/pages/AdminFormFieldConfiguration.tsx` | Complete. Lint PASS (0 errors / 3 expected warnings). Build PASS. |
| CLAUDE-JOIN-UI-003 | Enable Join form preview entrypoint in Studio | Claude | Added `join_lub` → `/join?preview=1` to the preview-path ternary in AdminFormStudio so the preview button opens the Join page in preview mode. Updated fallback toast to generic copy (not signup/sign-in only). | `src/pages/AdminFormStudio.tsx` | Complete. Lint PASS (0 errors / 3 expected warnings). Build PASS. |
| CLAUDE-UI-012 | Move Dashboard link from header bar into user dropdown menu | Claude | Removed standalone Dashboard button from desktop header and mobile nav; added Dashboard as first item in desktop user dropdown and removed duplicate from mobile menu. LayoutDashboard icon retained. | `src/components/Header.tsx` | Complete. Lint PASS (0 errors / 3 expected warnings). |
| COD-SIGNIN-FORM-001 | Bring Sign-In under Form Builder live configuration model | Codex | Added Sign-In Form Builder linkage end-to-end: seeded protected `signin` form + core auth fields, added live/draft Sign-In config RPCs, extended publish guard enforcement to Sign-In core fields, wired Sign-In runtime to load builder config with admin-only preview gate, and enabled Studio preview for Sign-In (`/signin?preview=1`) | `supabase/migrations/20260409235500_add_signin_form_builder_runtime_contracts.sql`, `src/lib/supabase.ts`, `src/pages/SignIn.tsx`, `src/pages/AdminFormStudio.tsx` | Complete. `db:migrations:audit` PASS (audit-only check), `db:migration:apply:single -- --version=20260409235500` PASS, lint PASS (0 errors / 3 expected warnings), build PASS, Phase1 readonly smoke PASS (3 passed / 12 skipped). |
| COD-FIELDLIB-SEARCH-001 | Smart search for Form Builder Field Library | Codex | Added ranked multi-attribute smart search for Field Library (label/key/section/type/validation/status/flags) with search feedback and filtered-result counts in UI | `src/pages/AdminFieldLibrary.tsx` | Complete. Lint PASS (0 errors / 3 expected warnings). Build PASS. Phase1 readonly smoke PASS (3 passed / 12 skipped). |
| COD-VAL-FORM-001 | Link Validation Settings with Form Configuration runtime | Codex | Linked Validation Settings to Form Builder + Signup runtime: Field Library create/edit now maps active validation rules, Signup runtime applies mapped regex rules on visible fields, and signup account-creation RPC now validates against published live field snapshot (not draft) while skipping inactive rules safely | `src/pages/AdminFieldLibrary.tsx`, `src/pages/SignUpV2.tsx`, `supabase/migrations/20260409213000_link_signup_runtime_validation_to_live_snapshot.sql` | Complete. Lint PASS (0 errors / 3 expected warnings). Build PASS. Migration `20260409213000` applied safely as single version. Phase1 readonly smoke PASS (3 passed / 12 skipped). |
| COD-SIGNUP-CUTOVER-002 | Remove pilot `/signup-v2` route and move preview to `/signup` | Codex | Removed public pilot route `/signup-v2`; Studio preview and preview-login return now use `/signup?preview=1` only, with primary runtime continuing on `/signup` | `src/App.tsx`, `src/pages/AdminFormStudio.tsx`, `src/pages/SignUpV2.tsx` | Complete. Lint PASS (0 errors / 3 expected warnings). Build PASS. Phase1 readonly smoke PASS (3 passed / 12 skipped). |
| COD-SIGNUP-CUTOVER-001 | Replace `/signup` with Signup V2 + rollback switch | Codex | Cut over `/signup` to Signup V2, preserved legacy page at `/signup-legacy`, and added env rollback switch (`VITE_SIGNUP_PRIMARY_MODE=legacy`) so primary signup can be flipped without route rewrites | `src/App.tsx`, `.env.example` | Complete. Lint PASS (0 errors / 3 expected warnings). Build PASS. Phase1 readonly smoke PASS (3 passed / 12 skipped). |
| COD-UI-011 | Remove field helper instructions from public Signup V2 form | Codex | Removed helper/instruction lines under each field from public `/signup-v2` so forms display only labels, inputs, and validation errors unless explicitly requested otherwise | `src/pages/SignUpV2.tsx` | Complete. Lint PASS (0 errors / 3 expected warnings). Build PASS. |
| COD-UI-010 | Tight Signup V2 visual sizing match to legacy signup layout | Codex | Refined `/signup-v2` visual sizing to match legacy `/signup`: card container width, heading/subheading typography scale, and core input/select icon paddings; preview/unpublish access-control behavior preserved | `src/pages/SignUpV2.tsx` | Complete. Lint PASS (0 errors / 3 expected warnings). Build PASS. Phase1 readonly smoke PASS (3 passed / 12 skipped). |
| COD-UI-009 | Signup V2 layout alignment to previous signup design | Codex | Updated `/signup-v2` to match the prior signup layout style (non-pilot heading/subtitle, core field leading icons, select chevron and left icon treatment, tighter card width) while preserving preview/unpublish access gating and submit behavior | `src/pages/SignUpV2.tsx` | Complete. UI-only change validated with `npm run lint` (0 errors / 3 expected warnings) and `npm run build` PASS. |
| CLAUDE-FB23-GATE-001 / CLAUDE-FB23-UNPUB-001 / CLAUDE-FB23-STATUS-001 | Preview access hard gate + Unpublish UI + badge/status alignment | Claude | (1) SignUpV2: hard access-control gate for `?preview=1` — `no_session` redirects to `/signin?next=`, `access_denied`/`load_failed` render blocking static screen, no silent live-config fallback; public form shows "unavailable" when field list is empty (unpublished). (2) AdminFormStudio: `UnpublishModal` sub-component, `handleUnpublish()` via `formBuilderV21Service.unpublishForm()`, Unpublish button in top bar (shown only for `manual_publish` / `legacy_seeded` origins), updated `livePublishEl` for all four origins including new `unpublished` (CloudOff icon, "Offline" label), fixed `statusEl` seed from `live_publish_origin` on load. (3) AdminFormBuilderV2: `getLivePublishBadge` updated with `unpublished` → "Offline" destructive style; label refreshes: `manual_publish` → "Live", `legacy_seeded` → "Live (seeded)". (4) SignIn: `?next=` post-login redirect (safe relative-URL only). | `src/pages/SignUpV2.tsx`, `src/pages/AdminFormStudio.tsx`, `src/pages/AdminFormBuilderV2.tsx`, `src/pages/SignIn.tsx` | Complete. Lint PASS (0 errors / 3 expected warnings). Build PASS. Phase1 readonly smoke PASS (3 passed / 12 skipped). |
| COD-FB23-BE-001 | Preview access hardening + explicit unpublish backend contracts | Codex | Shipped backend contracts for FB23 hardening: added `unpublish_form_builder_v2_with_session`, extended publish-status origin with `unpublished`, switched public signup runtime read path to live snapshot only (no draft fallback), and exported draft-preview error classification plus unpublish service method for Claude UI | `supabase/migrations/20260409182000_preview_unpublish_hardening.sql`, `src/lib/supabase.ts` | Complete. Targeted migration applied safely (`--version=20260409182000`). Lint PASS (0 errors / 3 expected warnings). Build PASS. Phase1 readonly smoke PASS (3 passed / 12 skipped). Public live RPC probe PASS (`signup_live_field_count=3`). Claude unblocked for `CLAUDE-FB23-GATE-001` and `CLAUDE-FB23-UNPUB-001`. |
| COD-PUBLISH-001 | Manual publish gate hardening + publish origin visibility | Codex | Added DB-level live snapshot write guard trigger so `form_config_v2_live_fields` is writable only during explicit publish RPC context; added session-based publish-status read contracts and surfaced manual vs legacy-seeded live origin metadata in Form Builder and Form Studio; legacy individual form pages intentionally retained for UAT deferment | `supabase/migrations/20260409133000_manual_publish_gate_and_origin_visibility.sql`, `src/lib/supabase.ts`, `src/pages/AdminFormBuilderV2.tsx`, `src/pages/AdminFormStudio.tsx`, coordination docs | Complete. Targeted migration applied safely (`--version=20260409133000`). Guard verified by blocked direct live-table update. Lint PASS (0 errors / 3 expected warnings). Build PASS. Phase1 readonly smoke PASS (3 passed / 12 skipped). |
| COD-FB23-001 | Form Builder + Signup preview consistency fixes (controlled select sources) | Codex | Implemented draft-preview contract for Signup (`preview=1` reads draft via session RPC), controlled select-option resolver across Studio/Signup preview/runtime, required-guard for unresolved select fields, and Field Library options input fix (parse on blur/save, not per keystroke) | `supabase/migrations/20260407121000_form_builder_preview_draft_and_select_guard.sql`, `src/lib/formFieldOptionResolver.ts`, `src/lib/supabase.ts`, `src/pages/SignUpV2.tsx`, `src/pages/AdminFormStudio.tsx`, `src/pages/AdminFieldLibrary.tsx` | Complete. Targeted migration applied safely (single version). Lint PASS (0 errors / 3 expected warnings). Build PASS. Phase1 readonly smoke PASS (3 passed / 12 skipped). |
| COD-FB22-UX-002 | Form Studio UX polish: sticky properties + clean preview + autosave | Codex | Fixed studio shell scrolling so properties pane stays visible while canvas scrolls; renamed top-bar action to `Preview`; removed manual `Save Draft` action and added automatic debounced draft save on form-structure edits; removed preview-only informational banner and preview-submit messaging from `/signup-v2` while still preventing preview-mode submissions | `src/pages/AdminFormStudio.tsx`, `src/pages/SignUpV2.tsx` | Complete. Lint PASS (0 errors / 3 expected warnings). Build PASS. Phase1 readonly smoke PASS (3 passed / 12 skipped) after known intermittent admin-denial retries. |
| COD-FB22-VERIFY-001 | Form Studio UX + publish integration verification | Codex | Verified Claude Studio redesign against user intent: dedicated studio route, new-tab launch from builder, 3-panel model (palette/canvas/properties), and publish control behavior; re-ran baseline gates | Targeted Playwright verification + baseline smoke + coordination docs | Complete. Targeted Playwright studio verification PASS (1/1), lint PASS (0 errors / 3 expected warnings), build PASS, Phase 1 readonly smoke PASS (3 passed / 12 skipped). |
| COD-FB22-BE-001 | Draft-vs-live publish backend workflow for Form Studio | Codex | Added live snapshot workflow so builder edits remain draft until explicit publish; runtime signup reads live snapshot; exported publish contract for studio UI; seeded initial live snapshot for existing forms | `supabase/migrations/20260406170000_add_form_builder_live_publish_workflow.sql`, `supabase/migrations/20260406171000_seed_initial_live_snapshot_form_builder.sql`, `src/lib/supabase.ts` | Complete. Applied both migrations via safe single-version push. Lint/build PASS. |
| CLAUDE-FB22-UI-001 | Form Studio UX redesign — 3-panel studio + publish | Claude | New `AdminFormStudio.tsx`: full-screen 3-panel builder at `/admin/form-studio/:formKey` (new tab, no sidebar). Left = field palette (search, in-form click-to-select, library click-to-attach). Center = true form canvas (exact public rendering, section groups, selectable fields with ring, hidden-field opacity, move/detach on hover). Right = properties inspector (type/section info, visibility + required toggles, order arrows, remove, locked guard, library link). Top bar: status pill (Draft/Unsaved/Saved/Published), Save Draft, Publish to Live with confirm modal. `AdminFormBuilderV2.tsx` updated: all edit entry points open studio via `window.open` + `target="_blank"`. Route registered outside AdminLayoutWrapper. | `src/pages/AdminFormStudio.tsx` (new), `src/pages/AdminFormBuilderV2.tsx`, `src/App.tsx` | Complete. Lint 0 errors / 3 expected warnings. Build PASS. Phase 1 readonly smoke PASS (3/12). |
| CLAUDE-FB21-UI-001/002/003 | Form Builder V2.1 UI slices | Claude | (1) AdminFormBuilderV2.tsx rewritten: row-layout form list, DropdownMenu per row (Edit/Clone/Archive), inline Create Form panel, Field Library link — uses `formBuilderV21Service.listForms()`. (2) AdminFormEditorV2.tsx created: split-pane at `/builder/:formKey`, left pane field config (visible/required toggles, up/down reorder, attach-from-library, detach, save settings), right pane sticky preview with "Preview Mode" banner — no submit. (3) AdminFieldLibrary.tsx created: `/library`, list/create/edit/archive field templates via `fieldLibraryV2Service`. (4) App.tsx: added 2 new routes. | `src/pages/AdminFormBuilderV2.tsx`, `src/pages/AdminFormEditorV2.tsx` (new), `src/pages/AdminFieldLibrary.tsx` (new), `src/App.tsx` | Complete. Lint 0 errors / 3 expected warnings. Build PASS. Phase 1 readonly smoke PASS (3/12). |
| COD-FB21-VERIFY-001 | Form Builder V2.1 post-UI integration verification | Codex | Verified Form Builder V2.1 end-to-end integration after Claude UI delivery: route access, editor preview marker, attach/detach path, reorder/save path, and field-library route behavior | Targeted Playwright verification + baseline checks + coordination docs | Complete. Targeted Playwright run PASS (1/1). `npm run lint` PASS (0 errors / 3 expected warnings), `npm run build` PASS, `npm run test:e2e:phase1:local` PASS (3 passed / 12 skipped). |
| COD-SIGNUPV2-001 | Signup/Form Builder alignment + re-add duplicate-key fix | Codex | Fixed Form Builder re-add failure by restoring soft-deleted field rows in create RPC instead of inserting duplicate keys; stabilized Add Existing Field type override + select options input wiring in builder UI | `supabase/migrations/20260406102000_fix_form_builder_readd_soft_deleted_fields.sql`, `src/pages/AdminFormBuilderV2.tsx`, coordination docs | Complete. Lint/build PASS. Phase1 readonly smoke PASS (3 passed / 12 skipped). Migration applied safely via single-version push. |
| COD-FB21-BE-001/002 | Form Builder V2.1 backend foundation + contract exports | Codex | Added centralized field library table + backfill for Signup/Join into builder domain and shipped builder/library RPC contracts (`list/get/create/clone/archive/attach/detach/reorder/settings/library CRUD`) with `_with_session` writes plus typed client service exports for Claude UI slices | `supabase/migrations/20260406143000_form_builder_v21_backend_foundation.sql`, `src/lib/supabase.ts`, coordination docs | Complete. `npm run db:migrations:audit` PASS; `npm run db:migration:apply:single -- --version=20260406143000` PASS; lint/build PASS; Phase1 readonly smoke PASS (3 passed / 12 skipped). Claude unblocked for FORM-BUILDER-V2.1 UI slices. |
| COD-FORM-ARCH-003 | Make section/validation read-only in Add Existing Field flow | Codex | In Form Builder existing-field add flow, removed editable section/validation controls and auto-applied mapped section/validation from selected field template; both are now informational only in UI | `src/pages/AdminFormBuilderV2.tsx`, coordination docs | Complete. Lint/build PASS. Phase1 readonly smoke PASS (3 passed / 12 skipped). |
| COD-FORM-ARCH-002 | Restrict Form Builder add-field to existing field library | Codex | Replaced freeform field creation in Form Builder with selection from existing project fields only (legacy Join configuration + existing V2 forms), with availability guards to prevent duplicate active field-key conflicts across forms | `src/pages/AdminFormBuilderV2.tsx`, coordination docs | Complete. Lint/build PASS. Phase1 readonly smoke PASS (3 passed / 12 skipped). |
| COD-FORM-ARCH-001 | Centralize form/field creation in Form Configuration | Codex | Implemented safer model: removed Signup per-form custom field creation entrypoint, added centralized Form Builder route/page to create forms and fields, and added backend service contracts for generic form list/config/create/delete | `supabase/migrations/20260405231500_add_form_builder_v2_centralized_management.sql`, `src/lib/supabase.ts`, `src/pages/AdminFormsList.tsx`, `src/pages/AdminSignupFormConfiguration.tsx`, `src/pages/AdminFormBuilderV2.tsx`, `src/App.tsx`, coordination docs | Complete. Migration applied safely as single targeted push (`--include-all` in isolated temp workdir containing only fetched remote migrations + target file). Lint/build PASS. Phase1 readonly smoke PASS (3 passed / 12 skipped). |
| COD-VAL-011 | Validation Rules active-status toggle UX clarity | Codex | Replaced confusing Active/Inactive status-pill action with an explicit ON/OFF switch and introduced a compact Actions menu for secondary actions (Edit), preserving existing toggle behavior and permissions | `src/pages/AdminValidationSettings.tsx`, coordination docs | Complete. Lint/build PASS. Phase1 readonly smoke PASS (3 passed / 12 skipped). |
| COD-VAL-010 | Fix Validation Settings rule disappearing on Active toggle | Codex | Root cause fixed by replacing admin validation-rules read path with session-based RPC that returns active + inactive rows. Toggling active now deactivates without disappearing from admin management view. | `supabase/migrations/20260405233000_add_validation_rules_admin_read_rpc_with_session.sql`, `src/lib/supabase.ts` | Complete. Migration applied via safe single apply. Verified DB state: rules were deactivated (not deleted). Lint/build PASS; Phase1 readonly smoke PASS (3 passed / 12 skipped). |
| COD-OPS-001 | Supabase migration automation hardening | Codex | Added safe CLI automation for migration audit and single-migration apply via `lub-private/supabase-cli.env`, including audit report artifacts under `lub-private/migration-audit` | `scripts/supabaseMigrations.mjs`, `package.json`, `docs/supabase_migration_automation.md` | Complete. `npm run db:migrations:audit` PASS, lint PASS (0 errors / 3 expected warnings). |
| COD-V2-VERIFY-001 | Signup V2 integration verification (post-Claude UI) | Codex | Applied Signup V2 migration safely to linked Supabase project using isolated temp workdir, validated all V2 RPCs are live, and verified admin/public Signup V2 routes render expected schema after migration | Supabase migration application, runtime RPC probes, targeted Playwright route checks, coordination docs | Complete. Migration applied: `20260405110000_create_signup_form_config_v2_foundation.sql`. Lint/build PASS. Phase1 readonly smoke PASS (3 passed / 12 skipped). Targeted verification PASS for `/admin/settings/forms/signup` and `/signup-v2`. |
| CLAUDE-V2-FORMS-002 | Admin Signup Form Configuration V2 UI | Claude | Replaced `/admin/settings/forms/signup` stub with full config UI: collapsible sections, per-field visible/required toggles (locked-field protection), field-type badges, delete custom fields, add custom field inline form, save/reset via `signupFormConfigV2Service` only | `src/pages/AdminSignupFormConfiguration.tsx` | Complete. Lint 0 errors / 3 expected warnings. Build PASS. |
| COD-V2-BE-001/002/003/004 | Signup Form Configuration V2 foundation + RPCs + service + runtime contracts | Codex | Implemented Signup V2 backend foundation (hybrid model): V2 form/field/submission tables, seeded protected core signup fields, `_with_session` admin write RPCs, client service exports, and pilot runtime `/signup-v2` submission path while keeping Join V1 and current `/signup` unchanged | `supabase/migrations/20260405110000_create_signup_form_config_v2_foundation.sql`, `src/lib/supabase.ts`, `src/lib/memberAuth.ts`, `src/pages/SignUpV2.tsx`, `src/App.tsx` | Complete. Lint PASS (0 errors, 3 expected warnings). Build PASS. Phase1 readonly smoke PASS (3 passed / 12 skipped). Claude unblocked for CLAUDE-V2-FORMS-002 via `signupFormConfigV2Service`. |
| CLAUDE-V2-FORMS-001 | Signup V2 card in AdminFormsList + route stub | Claude | Replaced "More Forms" placeholder with live Signup Form card linking to /admin/settings/forms/signup. Created AdminSignupFormConfiguration stub page (permission-gated, "coming soon" state, back-link to Form Configuration). Registered route in App.tsx. Join LUB card and route untouched. | `src/pages/AdminFormsList.tsx`, `src/pages/AdminSignupFormConfiguration.tsx` (new), `src/App.tsx` | Complete. Lint 0 errors / 3 expected warnings. Build PASS. Browser verified. |
| CLAUDE-APPEAR-005 | Card Shadow control + table/card shadow split | Claude | Added independent `--card-shadow` CSS variable; split `.shadow-sm` override from `--table-shadow` → `--card-shadow` (table `:has()` selectors retain higher specificity and continue applying `--table-shadow`); added `CARD_SHADOW_OPTIONS`, `getCardShadow`/`setCardShadow`/`applyCardShadow` to themeManager; added Card Shadow preset row in Appearance → Table Style; fixed remaining `shadow-md` surfaces in AdminFormFieldConfiguration, AdminValidationSettings, AdminFormsList → `shadow-sm` to link them to card shadow token. Verified independently settable in browser. | `src/index.css`, `src/lib/themeManager.ts`, `src/pages/admin/AdminAppearanceSettings.tsx`, `src/pages/AdminFormFieldConfiguration.tsx`, `src/pages/AdminValidationSettings.tsx`, `src/pages/AdminFormsList.tsx` | Complete. Lint 0 errors / 3 expected warnings. Build PASS. Browser verified. |
| COD-UI-008 | Form Configuration sidebar target fix | Codex | Corrected Settings > Form Configuration sidebar path to open the Form Configuration hub page (`/admin/settings/forms`) instead of Join LUB detail (`/admin/settings/forms/join-lub`) | `src/components/admin/AppSidebar.tsx` | Complete. Lint PASS (0 errors, 3 expected warnings). |
| COD-TECH-001 | Build chunk optimization | Codex | Added safe Rollup manual chunking strategy for vendor and app-domain modules to reduce oversized production bundles without behavior changes | `vite.config.ts` | Complete. Build PASS with no large chunk warning; lint PASS (0 errors, 3 expected warnings); Phase1 readonly smoke PASS (3 passed / 12 skipped). |
| COD-ENV-001 | Runtime/env verification hardening | Codex | Added non-invasive env/runtime verification tooling for required Supabase client vars, edge-email secret readiness, and optional live storage bucket checks; no UI or data-model behavior changes | `scripts/verifyRuntimeEnv.mjs`, `package.json`, `.env.example`, `docs/runtime_env_verification.md` | Complete. `npm run verify:env` PASS (warnings only for missing local edge secrets/service role), lint/build PASS, Phase1 local smoke PASS (3 passed / 12 skipped on retry). |
| CLAUDE-UI-005 | Application settings hub in Settings | Claude | Added admin Settings Hub route/page, sidebar Settings Hub entry, and back-links from Forms/Validation/Appearance/Directory Visibility/Payment Settings without backend/data changes | `src/App.tsx`, `src/components/admin/AppSidebar.tsx`, `src/pages/AdminSettingsHub.tsx`, `src/pages/AdminFormsList.tsx`, `src/pages/AdminValidationSettings.tsx`, `src/pages/admin/AdminAppearanceSettings.tsx`, `src/pages/AdminDirectoryVisibility.tsx`, `src/pages/AdminDashboard/PaymentSettings.tsx` | Complete. Lint/build pass; Phase1 local smoke 3 passed / 12 skipped. |
| CLAUDE-UI-006 | Redesign expanded member details layout in public Directory | Claude | Reworked only expanded member details panel with structured section-card layout and cleaner label/value presentation; visibility/session rules unchanged | `src/components/ExpandedMemberDetails.tsx` | Complete. Lint pass, build pass. |
| COD-UI-007 | Directory/Admin UI polish after expanded-details redesign | Codex | Removed Directory state-level "members on this page" display, improved OFF-state contrast/focus visibility for Directory Visibility toggles, and replaced Admin Registrations row View button with clickable member name action | `src/pages/Directory.tsx`, `src/pages/AdminDirectoryVisibility.tsx`, `src/pages/AdminRegistrations.tsx` | Complete. Lint pass, build pass. |
| CLAUDE-APPEAR-004B | Strict appearance-linkage audit (authenticated surfaces) | Claude | Authenticated-surface color/icon/spinner token cleanup pass | Admin/member authenticated pages and shared dashboard components | Complete locally. |
| CLAUDE-APPEAR-004 | Appearance conformance pass (third pass reverted by user direction) | Claude | Passes 1-2 retained; pass 3 reverted where it overreached intentional design choices | Authenticated portal files | Complete. |
| COD-APPEAR-003 | Hardcode appearance audit and safe replacements | Codex | Replaced low-risk hardcoded backgrounds/borders/text/table wrappers with tokenized styles | Admin/member/authenticated surfaces | Complete locally. |
| COD-JOIN-001 | Fix Join verification/submission loading-state regression | Codex | Corrected Verify/Submit loading-state crossover | `src/pages/Join.tsx` | Complete locally. |
| COD-DASH-001 | Fix dashboard Active Admin Users metric | Codex | Count admin-capable active users from `users` instead of `user_roles` row count | `src/hooks/useDashboardData.ts` | Complete. |
| COD-PAY-001 | Payment settings admin cleanup | Codex | Upload storage migration + session-wrapped payment-settings delete + admin delete action path | Supabase migrations + `src/pages/AdminDashboard/PaymentSettings.tsx` | Complete. |
| COD-SHADCN-001 | Tailwind v4 + shadcn/ui peer deps — build config | Codex | Upgraded Tailwind v3→v4, added @tailwindcss/vite plugin, installed radix-ui/clsx/twmerge/cva deps, added @/ path alias, deleted tailwind.config.js | `package.json`, `vite.config.ts`, `postcss.config.js`, `tsconfig.app.json`, `tsconfig.node.json` | Complete and committed (2fc2887). Build PASS. |
| CLAUDE-SHADCN-002 | shadcn/ui migration Phases 2–5 | Claude | CSS theme (Ocean Breeze), 19 shadcn UI components, AppSidebar (React Router), AdminLayout with SidebarProvider, admin page components migrated to shadcn Table/Badge/Card/Dialog/Input/Button | `src/index.css`, `index.html`, `src/lib/utils.ts`, `src/hooks/use-mobile.ts`, `src/components/ui/` (19 files), `src/components/admin/AppSidebar.tsx`, `src/components/admin/AdminLayout.tsx` | Complete and committed (2fc2887). Build PASS, lint 0 errors / 3 warnings (expected). |





