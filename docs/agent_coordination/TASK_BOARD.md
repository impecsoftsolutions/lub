# LUB Agent Task Board

Use this file as the strict shared queue between Codex and Claude Code.

Rules:
- One implementation slice has one owner.
- Claude owns UI-first slices by default.
- Codex owns backend/data/runtime slices by default.
- Do not start a new slice until `In Progress` is updated.
- Move a task to `Blocked` instead of improvising across ownership boundaries.
- Commit hygiene is mandatory: stage only the slice manifest files (no `git add .` in dirty worktrees), verify with `git diff --cached --name-only`, then commit scoped changes only.

## Ready

| ID | Title | Owner | Scope | Files / Domains | Dependency / Blocker |
|----|-------|-------|-------|-----------------|----------------------|
| COD-MSME-SHOWCASE-001 | MSME product showcase platform (free + member tiers) | Codex + Claude | Build a product display platform for MSME businesses. Not a marketplace: businesses list products, visitors submit inquiries, and member status unlocks richer limits/features. | new DB tables, file storage for product images, public product listing/detail, inquiry flow, member product management dashboard, admin moderation, tier gating logic | Product refinement needed with user before starting. |
| COD-MSME-ISSUES-001 | MSME issue intake, AI categorization, and representation workflow | Codex | Create a member-led issue submission platform with document upload, extraction, AI rephrasing/classification/tagging, and admin review/reporting. | signup/onboarding, document upload + OCR extraction, issue submission flow, AI settings/key wiring, issue taxonomy, admin review/analytics/reporting | Product refinement still needed for moderation, access model, and privacy/retention policy. |
| COD-PUBLIC-001 | Implement remaining public News page content | Codex | Replace placeholder public `news` route with real content. Events half completed by `CLAUDE-EVENTS-ACTIVITIES-IA-IMPLEMENT-008`. News half remains blocked because no backend content source is defined yet. | `src/pages/News.tsx`, supporting content/data source, routing/runtime verification | No backend content source identified yet. |
| COD-MEMBERS-EXPORT-002 | Low-priority export UX follow-ups | Codex | Deferred export-quality enhancements only after higher-priority product work: optional `Select all extras` / `Clear extras`, optional CSV export, optional remember-last-selection behavior. | `src/pages/AdminRegistrations.tsx`, `src/lib/xlsxExport.ts`, export utility/runtime surface | Lowest priority by user direction. |

## In Progress

| ID | Title | Owner | Scope | Files / Domains | Dependency / Blocker |
|----|-------|-------|-------|-----------------|----------------------|

## Blocked

| ID | Title | Owner | Scope | Files / Domains | Dependency / Blocker |
|----|-------|-------|-------|-----------------|----------------------|

## Completed Recently

| ID | Title | Owner | Scope | Files / Domains | Outcome |
|----|-------|-------|-------|-----------------|---------|
| COD-EVENTS-BADGE-CAMERA-SCANNER-072 | Admin badge check-in camera QR scanner | Codex | Added camera-based QR scanning to the existing admin badge check-in page while preserving manual code/URL lookup as fallback. Scanner starts from user click, requests rear camera, detects QR payload continuously, auto-normalizes to badge code, then reuses existing lookup + attendance path. View-only users can scan/lookup; manage users can mark attendance. No DB migration or function change required. | `src/pages/AdminEventCheckin.tsx` | Complete. Validation PASS: lint (0 errors / 3 expected warnings), build PASS, phase1 readonly smoke PASS (3 passed / 12 skipped). First readonly run failed at admin login with known auth flake; immediate retry passed. |
| COD-EVENTS-BADGE-LAYOUT-DESIGNATION-QR-PROFESSION-BADGE-DOWNLOAD-AADHAAR-071 | Event badge layout/designation/QR + custom profession options + post-registration badge download + full Aadhaar reports | Codex | Completed combined Events follow-up. Badge renderer now draws uploaded template at full opacity without generated white overlay/card, prints designation only (no profession fallback), and QR encodes only the badge code. Profession dropdown options are configurable per event via admin Event form and enforced server-side. Public registration now receives `badge_code` and shows website-hosted badge open/download links (`/events/badge/:code`) without exposing Supabase function URL in the browser address bar. Admin registrations table/export now show full Aadhaar number for organisers. | `supabase/functions/event-badge-download/index.ts`, `supabase/migrations/20260507018000_events_custom_profession_options_070.sql`, `supabase/migrations/20260507019000_events_registration_badge_return_071.sql`, `supabase/migrations/20260507019100_events_registration_badge_return_schema_hotfix_071x.sql`, `src/App.tsx`, `src/pages/EventBadgeDownload.tsx`, `src/pages/ActivityDetail.tsx`, `src/pages/AdminEventForm.tsx`, `src/pages/AdminEventRegistrations.tsx`, `src/lib/supabase.ts` | Complete. Codex applied migrations, deployed `event-badge-download --no-verify-jwt`, verified live badge artifact `artifacts/probe_069_badge_final.png`, verified `/events/badge/NO7026` website route, verified public RSVP returns `badge_code` (probe returned `TO5701`, row deleted), verified custom profession option acceptance, and wrote runtime artifact `artifacts/probe_071_runtime.json`. Validation PASS: lint/build; Phase 1 readonly smoke PASS on retry (3 passed / 12 skipped; first run hit known admin-dashboard loading flake). |
| COD-EVENTS-REGISTRATION-BADGE-EXPORT-AADHAAR-068 | Export fix, rotated Aadhaar PDF, badge template rendering, check-in page, badge search | Claude + Codex | Combined slice completed: badge-code search, manual/LUB-URL badge check-in page, check-in attendance RPCs, rotated Aadhaar PDF browser rendering before transient extraction, uploaded template full-opacity badge rendering, LUB-domain QR route to check-in, and permissioned non-admin export verification. Absorbs `COD-EVENTS-BADGE-AI-TEMPLATE-RENDER-065`, `COD-EVENTS-BADGE-SCANNER-ATTENDANCE-066`, and `COD-EVENTS-RSVP-BADGE-SEARCH-067`. | `supabase/migrations/20260507017000_events_badge_checkin_068.sql`, `src/pages/AdminEventRegistrations.tsx`, `src/pages/AdminEventCheckin.tsx`, `src/App.tsx`, `src/lib/supabase.ts`, `src/lib/pdfImageRender.ts`, `supabase/functions/extract-event-aadhaar/index.ts`, `supabase/functions/event-badge-download/index.ts` | Complete. Codex applied migration via `supabase db push --linked`, deployed `extract-event-aadhaar --no-verify-jwt` and `event-badge-download --no-verify-jwt`, set `PUBLIC_SITE_URL=https://lub.org.in`, and ran runtime artifact `artifacts/probe_068_runtime.json`. PASS: badge search `9519`, rotated Aadhaar PDF UI extraction (`Rajulapati` / expected given name, confidence 0.98, last4 only stored in artifact), no extraction persistence, badge check-in lookup/manage/idempotency/view-only denial, full-opacity template badge render, valid permissioned non-admin XLSX export with Badge No. Validation PASS: lint/build/phase1 readonly smoke. |
| COD-EVENTS-REGISTRATION-AADHAAR-EXPORT-BADGE-LIST-064 | Aadhaar name split fix, block reorder, Badge No. column, Excel export fix | Claude + Codex | Four combined fixes: deterministic first-word=surname / rest=given split in `sanitizeResult`; three-way extraction state (`success` / `partial` / `error`); Aadhaar Card block moved immediately after Day-of-visit; Badge No. column added to Admin Event Registrations and XLSX export; XLSX export hardened for permissioned non-admin users. Codex closeout also fixed `extraction_failed` to return structured HTTP 200 JSON and aligned the public UI error copy. | `supabase/functions/extract-event-aadhaar/index.ts`, `src/pages/ActivityDetail.tsx`, `src/pages/AdminEventRegistrations.tsx`, `src/lib/xlsxExport.ts` | Complete. Codex redeployed `extract-event-aadhaar --no-verify-jwt` (active version 5) and ran runtime artifact `artifacts/probe_064_runtime.json`. PASS: direct function split (`Tulasi` / `Yogish Chandra`), public UI success/partial/error states, Badge No. table column with code `LUBAPYU9519`, permissioned non-admin XLSX export valid (`PK` signature, Badge No. header/code present), lint/build/phase1 readonly smoke. |
| COD-EVENTS-AADHAAR-DOC-AUTOFILL-063B | Event registration Aadhaar upload/camera extraction without source storage | Codex + Claude | Public event registration now supports transient Aadhaar image/PDF extraction through `extract-event-aadhaar`. Visitors can upload Aadhaar or use camera capture when Aadhaar collection is enabled; extracted Aadhaar/name fields autofill without overwriting typed surname/given name. Codex hardened runtime rate limiting with a DB-backed opaque SHA-256 key counter and public no-JWT function config. No Aadhaar source file bytes, OCR text, Aadhaar numbers, or names are persisted by the function. | `supabase/functions/extract-event-aadhaar/index.ts`, `supabase/migrations/20260507016000_events_aadhaar_extract_rate_limit_063b.sql`, `supabase/config.toml`, `src/lib/supabase.ts`, `src/pages/ActivityDetail.tsx` | Complete. Codex deployed `extract-event-aadhaar`, applied rate-limit migration, redeployed with `--no-verify-jwt`, and ran runtime artifact `artifacts/probe_063b_runtime.json`. PASS: happy path extracted 12-digit Aadhaar/name/DOB with confidence 0.98 (artifact masked), no DB/storage source persistence (`event_assets` 3->3, storage objects 26->26), no-file/unsupported/oversize/AI-disabled errors correct, rate limit returns `[200,200,200,200,200,429]`, and public UI shows Upload Aadhaar/Take Photo/privacy notice with no-overwrite autofill. Validation PASS: lint/build/phase1 readonly smoke. Caveat: installed Supabase CLI has no functions logs command, so log redaction was verified by source inspection and redacted artifact rather than live log grep. |
| COD-EVENTS-BADGE-DESIGN-AND-LIVE-RENDER-063A | Badge sample AI analysis + live dynamic badge rendering | Codex + Claude | Completed combined badge design slice (`061` + `062`). Added analyzer metadata contract, sample upload parity/status UI, AI analysis trigger/polling, retry/re-analyze/legacy Analyze now states, dynamic badge rendering from latest template/sample/analysis, and no-store/ETag cache behavior. | `supabase/migrations/20260507015000_events_badge_design_analysis_063a.sql`, `supabase/functions/analyze-event-badge-sample/index.ts`, `supabase/functions/event-badge-download/index.ts`, `supabase/functions/event-media-upload/index.ts`, `src/lib/supabase.ts`, `src/pages/AdminEventForm.tsx` | Complete. Codex applied migration and deployed functions. Runtime artifact `artifacts/probe_063a_runtime.json`: fresh upload pending->complete, replace re-analysis with new source/time, remove clears metadata, legacy Analyze now works, live badge keeps same code (`TP2472`) and one DB row while PDF hash changes, direct/mobile downloads return `Cache-Control: no-store, must-revalidate` and ETag, public payload excludes badge reference assets/analysis and Aadhaar. Validation PASS: lint/build/phase1 readonly smoke. Hotfix after closeout: fixed `AdminEventForm.tsx` callback initialization order (`triggerSampleAnalysis` before `handleAssetUpload`) to resolve authenticated edit-route blank page; targeted browser probe and lint/build/phase1 PASS. Follow-up sample-upload fix: badge references now bypass web-image compression and accept only PDF/JPEG/PNG, preventing PNG->WebP rejection; lint/build/phase1 PASS. Follow-up thumbnail fix: badge template/sample cards now show image thumbnails for JPEG/PNG and PDF preview tiles for PDF references; lint/build/phase1 PASS. Retention clarification: active badge template/sample are stored until explicitly replaced or removed by an admin; analysis/logout do not delete them, and the admin form now states this. Caveats: QR image was not decoded locally; provider-outage retry not forced to avoid changing live AI settings. |
| COD-EVENTS-REGISTRATION-COMPLETE-059 | Events registration completion: Aadhaar + approved-member prefill + Excel export | Codex + Claude | Single combined Events registration slice folding `055` Aadhaar, `056` approved-member prefill, and `057` registration export. Added real Aadhaar collect/require event flags, normalized nullable `event_rsvps.aadhaar_number`, public/admin/read/submit RPC support, approved-member prefill RPC, admin RSVP matrix row, public Aadhaar input with required/invalid validation, masked Aadhaar in admin list/export, and current-filter Excel export. | `supabase/migrations/20260507014000_events_registration_complete_059.sql`, `src/lib/supabase.ts`, `src/lib/aadhaar.ts`, `src/lib/xlsxExport.ts` (reuse), `src/pages/AdminEventForm.tsx`, `src/pages/ActivityDetail.tsx`, `src/pages/AdminEventRegistrations.tsx` | Complete. Codex applied migration via `supabase db push --linked`; validation PASS (`lint`, `build`, `phase1 readonly smoke`). Runtime probes PASS: blank Aadhaar -> `aadhaar_required`, invalid -> `invalid_aadhaar`, spaced valid value stored normalized, public slug payload exposes only flags, admin RSVP RPC returns raw value for masking, browser registrations list shows `XXXX XXXX 9012`, Excel export includes masked Aadhaar and not raw value, approved-member prefill returns available profile fields and badge mobile default. Probe event cleaned up. |
| COD-EVENTS-RSVP-VIEW-EVENT-ACCESS-058 | RSVP-view users can see Events list/detail needed for registrations | Codex | Hotfix Events admin read RPC authorization so users with `events.rsvp.view` / `events.rsvp.manage` are not blocked by `events.view` checks when listing events and opening event detail for registrations. | `supabase/migrations/20260507013000_events_rsvp_view_event_access_hotfix.sql` | Complete. Migration applied via `supabase db push --linked` on 2026-05-06; user verification confirmed Events section now loads for RSVP-view user and registrations drill-down works. |
| COD-ADMIN-ACCESS-ENTRYPOINT-FOR-PERMISSIONED-NON-ADMINS-056 | Show + admit Admin Panel for non-admin users with at least one admin permission | Claude + Codex | Runtime-closeout complete after 056X DB hotfix and 056Y UI parity fixes. Persona matrix PASS: (A) super_admin sees Header `Admin Panel`, enters `/admin/dashboard`, full sidebar; (B) permissioned non-admin (`narendranath547@gmail.com`) now sees Header `Admin Panel`, enters `/admin/dashboard`, sees event-scoped admin navigation (`Events` visible, `Activities` hidden without `activities.view`), can open `/admin/content/events` and `/admin/content/events/:id/registrations`, and is denied on `/admin/administration/users`; (C) pure member sees no admin link and is redirected from `/admin/*` to `/dashboard`. | `src/lib/adminAccess.ts`, `src/components/Header.tsx`, `src/components/admin/AdminLayout.tsx`, `src/lib/permissionService.ts`, `src/components/admin/AppSidebar.tsx`, `src/pages/AdminEvents.tsx`, `supabase/migrations/20260507012000_fix_get_user_permissions_056x.sql` | Complete. End-to-end behavior aligned: discoverability + admission + least-privilege navigation. |
| COD-ADMIN-ACCESS-PARITY-AND-SIDEBAR-SCOPE-056Y | Header parity + sidebar scope for permissioned non-admin users | Claude + Codex | Closed. Fixed service-layer permission row normalization (`permission_code` -> `code`) so permission checks no longer fail silently for non-super-admin users. Expanded Events/Activities sidebar logic so RSVP-only users see the `Events` child without exposing unrelated modules; updated AdminEvents gate to allow `events.view OR events.rsvp.view OR events.rsvp.manage`. | `src/lib/permissionService.ts`, `src/components/admin/AppSidebar.tsx`, `src/pages/AdminEvents.tsx` | Complete. Runtime probe confirmed persona-B link visibility and page access now work as intended. |
| COD-ADMIN-ACCESS-PERMISSION-FUNCTION-HOTFIX-056X | Hotfix: `get_user_permissions` UNION ORDER BY error | Claude + Codex | Runtime closeout completed. Applied migration `20260507012000_fix_get_user_permissions_056x.sql` via `supabase db push --linked`. SQL sanity confirmed function executes and returns expected counts (`narendranath547@gmail.com` -> 20, `yogish@gmail.com` -> 65, `kishoresindiri@gmail.com` -> 0) with no UNION/ORDER BY error. This removed the database exception path in PermissionContext and unblocked 056Y UI parity verification. | `supabase/migrations/20260507012000_fix_get_user_permissions_056x.sql` | Complete (DB hotfix). |
| COD-EVENTS-RSVP-SPLIT-NAME-AND-BADGE-NAME-OPTIONS-054 | RSVP surname/given-name split + badge name options + mobile overflow fix | Claude + Codex | Runtime closeout complete. Applied migration `20260507011000` and redeployed `event-badge-download`. Verified new `submit_event_rsvp` signature with `p_surname`/`p_given_name`, server-side `full_name` composition, idempotency unchanged (`duplicate=true` on same email), and backward compatibility (`p_full_name`-only still works). Verified `get_event_rsvps_with_session` returns `surname` and `given_name`. Verified badge renderer options live: include-surname OFF shows given name only (`Aarav`), max-chars truncation works (`VeryLongSï¿½` at max 10), font-size option changes output (different PDF hashes for 14 vs 30), and legacy badges still render via `full_name` fallback. Verified mobile `/admin/content/events/:id/edit` at `375x812` has no horizontal overflow (`docScrollWidth=375`, `bodyScrollWidth=375`). Probe rows/assets cleaned up after verification. | `supabase/migrations/20260507011000_events_rsvp_split_name_badge_name_options_054.sql`, `supabase/functions/event-badge-download/index.ts`, `src/lib/supabase.ts`, `src/pages/AdminEventForm.tsx`, `src/pages/ActivityDetail.tsx` | Complete. Codex ran `supabase db push --linked` + `supabase functions deploy event-badge-download --no-verify-jwt`; runtime probes PASS. |
| COD-EVENTS-DESIGNATION-CODE-V3-TEMPLATE-UPLOADS-052 | Designation field, 6-char badge code, badge design references upload, mobile header polish | Claude + Codex | Closed runtime for the full 052 batch. Applied migration `20260507010000`, deployed `event-media-upload`, verified new badge issue codes match `^[A-Z]{2}[0-9]{4}$` (example issued code: `MH4957`), verified designation enforcement (`designation_required` when collected+required; successful insert with `designation='Managing Director'` when supplied), verified collect-off strips posted designation (`designation` stored NULL), verified `badge_template` + `badge_sample` uploads and singleton replacement behavior in `event_assets`, verified public `get_event_by_slug` assets exclude the two badge reference kinds, and verified mobile `/admin/content/events/:id/edit` header at `375ï¿½812` (`Edit Event` width 343px; action buttons wrap below title). Also confirmed badge QR payload path in renderer points to `event-badge-download?code=<badge_code>` and badge code uniqueness remains enforced by DB unique index. | `supabase/migrations/20260507010000_events_designation_badge_code_v3_template_uploads_052.sql`, `supabase/functions/event-media-upload/index.ts`, `src/lib/supabase.ts`, `src/pages/AdminEventForm.tsx`, `src/pages/ActivityDetail.tsx`, `src/pages/AdminEventRegistrations.tsx`, `src/components/ui/PageHeader.tsx` | Complete. Codex ran `supabase db push --linked` + `supabase functions deploy event-media-upload`; runtime probes PASS. |
| COD-EVENTS-VISITOR-DELETE-050X | Hotfix: PostgREST cache miss on `delete_event_rsvp_with_session` | Claude + Codex | Closed production `PGRST202` issue where admin delete appeared to do nothing because PostgREST could not find the RPC in schema cache. Added idempotent migration to re-`CREATE OR REPLACE` RPC and trigger `NOTIFY pgrst, 'reload schema'`; service now surfaces `rpc_not_found` with actionable guidance. | `supabase/migrations/20260507009000_events_visitor_delete_pgrst_reload_050.sql`, `src/lib/supabase.ts` | Complete. Codex applied migration (`supabase db push --linked`) and confirmed RPC is PostgREST-visible via direct REST call returning structured app envelope (`invalid_session`) instead of `PGRST202`. |
| COD-EVENTS-VISITOR-DELETE-050 | Admin delete of an event visitor registration | Claude + Codex | Added secure visitor delete path in Events registrations admin screen with destructive confirmation and clear cascade messaging. Server-side delete RPC enforces `events.rsvp.manage`; delete is transactional and relies on FK cascade to remove linked badge and delivery rows. | `supabase/migrations/20260507008000_events_visitor_delete_and_badge_template_050_051.sql`, `src/lib/supabase.ts`, `src/pages/AdminEventRegistrations.tsx` | Runtime apply complete: Codex ran `supabase db push --linked`. UI destructive action and permission/cascade behavior should be confirmed in a manage-permission admin session before final production signoff. |
| COD-EVENTS-BADGE-TEMPLATE-PREVIEW-051 | Per-event badge template selection with previewable options | Claude + Codex | Added four badge templates (`classic_corporate`, `minimal_clean`, `bold_header`, `compact_info`) with per-event selection persisted in `events.ai_metadata.badge_template_key`. `event-badge-download` now uses a template dispatcher and supports `?preview=1&event_slug=&template=` sample rendering for template selection. | `supabase/functions/event-badge-download/index.ts`, `src/lib/supabase.ts`, `src/pages/AdminEventForm.tsx`, `supabase/migrations/20260507008000_events_visitor_delete_and_badge_template_050_051.sql` | Runtime deploy complete: Codex redeployed with `supabase functions deploy event-badge-download --no-verify-jwt` and verified all 4 preview endpoints return `HTTP 200 application/pdf`. |
| COD-EVENTS-BADGE-QUALITY-049 | Badge code format `LUBAP[A-Z]{2}[0-9]{4}` + QR + center-aligned, date-only PDF + LUB branding | Claude + Codex | Completed end-to-end. Added DB-side v2 badge code generator and trigger update for new badge issuance, preserving legacy codes. Updated badge renderer to include QR, center-aligned layout, date-only display (no time), no location, and LUB footer branding. | `supabase/migrations/20260507007000_events_badge_code_and_layout_049.sql`, `supabase/functions/event-badge-download/index.ts` | Complete. Codex applied migration (`supabase db push --linked`) and redeployed function (`supabase functions deploy event-badge-download --no-verify-jwt`). Live probe returned `HTTP 200 application/pdf` via mobile lookup. |
| COD-EVENTS-BADGES-048 | Auto-issue + email + download event registration badges (4ï¿½6 PDF) | Claude + Codex | Completed end-to-end. Confirmed RSVP inserts now auto-issue one `event_badges` row via DB trigger and queue `event_badge_deliveries` when email exists. Admin registrations page now shows badge download + delivery status + send/retry action. Public event page includes mobile-based badge retrieval. | `supabase/migrations/20260507006000_events_badges_048.sql`, `supabase/functions/event-badge-download/index.ts`, `supabase/functions/event-badge-deliver/index.ts`, `src/lib/supabase.ts`, `src/pages/AdminEventRegistrations.tsx`, `src/pages/ActivityDetail.tsx`, `supabase/config.toml` | Complete. Codex applied migration (`supabase db push --linked`) and deployed functions (`supabase functions deploy event-badge-download`, `supabase functions deploy event-badge-deliver`). Follow-up runtime hotfix: redeployed `event-badge-download` with `--no-verify-jwt` and added persistent config guard `[functions.event-badge-download] verify_jwt = false` to fix public/direct badge download auth error. Local validation PASS: lint/build/phase1 readonly smoke (3 passed / 12 skipped). |
| COD-EVENTS-RSVP-REQUIRED-UX-FLIP-047 | Event registration matrix UX: `Optional` -> `Required` semantics | Claude + Codex | Flipped admin RSVP matrix wording and checkbox behavior so `Required` maps directly to `require_*` booleans while keeping `Collect` visibility behavior unchanged. Added collect-off reset of corresponding `require_*` flags to avoid stale disabled-checked states. No backend contract rename and no migration. | `src/pages/AdminEventForm.tsx` | Complete. Validation reported PASS for lint/build/phase1 readonly smoke. Public form semantics remain unchanged and continue to honor `require_*` flags with existing Mobile/Organisation/Note labels. |
| COD-EVENTS-BANNER-PUBLIC-COMPRESS-046 | Event banner public render fix + auto image compression under 1 MB | Claude + Codex | Completed end-to-end. Fixed missing banner on public `/events` listing by adding `banner_image_url` to `get_published_events`, mapping it through `PublicEvent`/`Events.tsx`, and rendering event cards from the stored event banner URL. Added image auto-compression helper (`compressImageToBudget`) targeting under 1 MB for banner/flyer/gallery uploads while preserving dimensions/aspect ratio; documents intentionally bypass compression. Edge function `event-media-upload` cap raised to 16 MB for compression floor headroom. | `supabase/migrations/20260507005000_events_listing_banner_046.sql`, `supabase/functions/event-media-upload/index.ts`, `src/lib/imageProcessing.ts`, `src/lib/supabase.ts`, `src/pages/Events.tsx`, `src/pages/AdminEventForm.tsx` | Complete. Codex applied migration (`supabase db push --linked`), deployed function (`supabase functions deploy event-media-upload`), and ran runtime browser probe with live evidence: `BANNER_BYTES=2774829->840618`, `FLYER_BYTES=2450454->741558`, `DOC_BYTES=1433631->1433631`, `CARD_BANNER_NATURAL_WIDTH=2800`, `DETAIL_BANNER_NATURAL_WIDTH=2800`. |
| COD-EVENTS-AI-DESCRIPTION-LIMIT-045 | Cap AI-generated event description to 100 words | Claude + Codex | Completed end-to-end. Added deterministic server-side word cap (`MAX_DESCRIPTION_WORDS = 100`) in `draft-event-content` for draft mode description only; tightened prompt instruction; added admin helper text that AI description is intentionally short. | `supabase/functions/draft-event-content/index.ts`, `src/pages/AdminEventForm.tsx` | Complete. Codex deployed `draft-event-content` and ran live authenticated probes: `draft` mode returns populated fields with `description <= 100 words`; `draft_whatsapp` response shape unchanged (`data.whatsapp_invitation_message`); `extract_fields` response shape unchanged. No DB migration required. |
| COD-EVENTS-RSVP-OPTIONAL-EMAIL-044 | Event registration: make Email configurable (collect + optional/required) | Codex | Completed end-to-end. Admin RSVP matrix now allows Email like other fields (Collect/Optional), public Event registration form conditionally shows/requires Email, and backend enforces `collect_email`/`require_email` from event metadata. Added migration to drop NOT NULL on `event_rsvps.email`, expose new RSVP flags in `get_event_by_slug`, and update `submit_event_rsvp` validation/idempotency for null-email registrations. | `src/pages/AdminEventForm.tsx`, `src/pages/ActivityDetail.tsx`, `src/pages/AdminEventRegistrations.tsx`, `src/lib/supabase.ts`, `supabase/migrations/20260507004000_events_rsvp_optional_email_044.sql` | Complete. Migration applied remotely via `supabase db push --linked`. Validation PASS: lint (0 errors / 3 warnings), build PASS, Phase 1 readonly smoke PASS (3 passed / 12 skipped). |
| COD-EVENTS-REGISTRATION-MEDIA-041X | Event assets delete RPC runtime hotfix | Codex | Runtime closeout uncovered a production defect: `delete_event_asset_with_session` attempted direct `DELETE` on `storage.objects`, blocked by storage trigger. Added SQL hotfix migration to make delete RPC remove DB rows + clear banner pointers without direct storage-table delete, returning `storage_path` for optional async cleanup. | `supabase/migrations/20260507001000_event_asset_delete_rpc_hotfix.sql` | Complete. Migration applied remotely during 041 closeout; delete RPC now succeeds and removes event asset rows reliably. |
| COD-EVENTS-REGISTRATION-MEDIA-041 | Events: registration list w/o edit mode + multi-day visit date + day-wise capacity + banner & files upload | Claude + Codex | Completed end-to-end. Codex applied migration, deployed `event-media-upload`, and ran runtime probes for per-day capacity/day selection logic, multi-day `visit_date_required`, single-day auto-assignment, registrations list load, asset upload/render/download flow, and permission denials. | `supabase/migrations/20260507000000_events_registration_media_041.sql`, `supabase/functions/event-media-upload/index.ts`, `src/lib/supabase.ts`, `src/pages/AdminEvents.tsx`, `src/pages/AdminEventForm.tsx`, `src/pages/AdminEventRegistrations.tsx`, `src/pages/ActivityDetail.tsx`, `src/App.tsx` | Complete. Runtime probes passed, including media upload for banner/flyer/document and public document URL fetch. |
| COD-EVENTS-AI-DATES-SHARE-040A-HOTFIX | Events AI date extraction reliability + publish-time RSVP share package | Claude + Codex | Completed end-to-end. Improved Event Brief date detection for range patterns and added deterministic fallback parser defaults (10:00/17:00) with non-PII `date_outcome` breadcrumbs. Added publish-only Share RSVP panel (copy/open URL, copy message, WhatsApp share, AI generate for RSVP share text). No DB migration in this hotfix. | `supabase/functions/draft-event-content/index.ts`, `src/pages/AdminEventForm.tsx` | Complete. Codex redeployed edge function and ran live probes: failing brief now returns non-null `start_at/end_at` (`2026-04-16T10:00:00+05:30` / `2026-04-17T17:00:00+05:30`) with `ai_date_detected`; no-date brief returns null dates with `no_date_detected`; `draft_whatsapp` returns message-only payload; invalid token returns `session_invalid`. |
| COD-EVENTS-NEXT-040A | Events RSVP field expansion + WhatsApp manual-trigger generation | Claude + Codex | Completed end-to-end. Added RSVP fields (`gender`, `meal_preference`, `profession`) with server-side enum validation, per-event collect toggles (`collect_gender`, `collect_meal`, `collect_profession`) with required-when-collected enforcement, and a dedicated `draft_whatsapp` AI generation path. Admin form no longer auto-applies WhatsApp text from generic brief generation and now uses an explicit button with overwrite confirm. | `supabase/migrations/20260506000000_events_rsvp_fields_v2.sql`, `supabase/functions/draft-event-content/index.ts`, `src/lib/supabase.ts`, `src/pages/AdminEventForm.tsx`, `src/pages/ActivityDetail.tsx` | Complete. Codex applied migration and redeployed edge function. Runtime probes passed: `gender_required`, `invalid_meal_preference`, valid row persistence for new fields, `get_event_by_slug` returns collect flags, admin RSVP list returns new columns, `draft_whatsapp` valid invoke returns only `whatsapp_invitation_message`, invalid token returns `session_invalid`. |
| COD-EVENTS-RSVP-BRIDGE-MAPS-WHATSAPP-039 | Events RSVP + Event?Activity bridge + venue map + AI WhatsApp invite | Claude + Codex | Completed end-to-end. Added RSVP schema/RPCs/permissions, event map link and WhatsApp invitation fields, idempotent Event?Activity bridge, admin import/bridge actions, and public event RSVP UI/actions. | `supabase/migrations/20260505000000_events_rsvp_bridge_maps_whatsapp.sql`, `supabase/functions/draft-event-content/index.ts`, `src/lib/supabase.ts`, `src/pages/AdminEventForm.tsx`, `src/pages/AdminEvents.tsx`, `src/pages/AdminActivities.tsx`, `src/pages/ActivityDetail.tsx` | Complete. Codex applied migration and redeployed edge function. Runtime probes passed: capacity handling (`confirmed` then `capacity_full`), member-only no session (`permission_denied`), past deadline (`rsvp_deadline_passed`), idempotent bridge (`reused: true` on second call), public slug payload includes `venue_map_url` + `whatsapp_invitation_message`, and editor status-update denied (`permission_denied`). |
| COD-EVENTS-CMS-AI-AUTOFILL-038 | Event Brief AI autofill + slug UX overhaul + agenda public toggle | Claude + Codex | Completed end-to-end on top of 032. Added Event Brief AI workflow, source-doc attach support, read-only-by-default slug with edit/check/reset flow, show-agenda-publicly toggle, server-side slug lock enforcement, public agenda gating, and pagination fixes in events read RPCs. Added edge function `draft-event-content` with `_with_session` permission enforcement. Follow-up update increased attachment limits to 5 files, 30 MB each, 150 MB total. Runtime closeout applied migrations, deployed function, and seeded `ai_runtime_settings` key `event_drafting` from existing normalization runtime profile. | `supabase/migrations/20260504000000_events_ai_autofill_and_slug_lock.sql`, `supabase/migrations/20260504010000_seed_event_drafting_ai_runtime.sql`, `supabase/functions/draft-event-content/index.ts`, `src/pages/AdminEventForm.tsx`, `src/lib/supabase.ts` | Complete. Local validation PASS (`lint`, `build`, `phase1 readonly smoke`). Runtime probes PASS for callable slug RPC and callable edge function (`invalid_session` / `session_invalid` checks). |
| COD-EVENTS-CMS-032 | Events CMS full implementation (schema + RPC + admin Events + unified public Events & Activities) | Codex | Completed end-to-end Events domain. Added dedicated events table, events.* permissions, secure _with_session CRUD/publish/archive/delete RPCs, public read RPCs with member-only visibility handling, service-layer eventsService, real admin Events list/form routes, and public /events integration that now reads both Events + Activities. /events/:slug now resolves Event first then Activity fallback. | supabase/migrations/20260503120000_events_cms_full.sql, src/lib/supabase.ts, src/pages/AdminEvents.tsx, src/pages/AdminEventForm.tsx, src/pages/Events.tsx, src/pages/ActivityDetail.tsx, src/components/admin/AppSidebar.tsx, src/App.tsx | Complete in repo. Validation PASS: lint (0 errors / 3 warnings), build PASS, Phase 1 readonly smoke PASS (3 passed / 12 skipped). |

| CLAUDE-SMART-UPLOAD-GST-CANDIDATES-017 | Smart Upload - Import button placement, GST Trade/Legal candidates, GST full_name fallback | Claude + Codex | Added `field_options` support for extraction results, moved `Import Data` below queued docs, surfaced Trade/Legal company-name candidate chips in `/join`, and added GST `full_name` fallback with Aadhaar precedence preserved. | `supabase/functions/extract-document/index.ts`, `src/lib/documentExtraction.ts`, `src/components/SmartUploadDocument.tsx`, `src/pages/Join.tsx` | Complete. Codex deployed `extract-document`; live invoke of `AA370425004153O_RC07042025.pdf` returned HTTP 200, `is_readable: true`, `detected_type: gst_certificate`, default Trade Name company value, GST `full_name`, and Trade+Legal options. Lint/build/Phase 1 readonly smoke PASS on 2026-04-26. |
| CLAUDE-SMART-UPLOAD-PDF-DATE-016 | Smart Upload deterministic GST PDF fallback + portal date display in review/conflict/summary | Claude + Codex | Added deterministic GST REG-06 text fallback in `extract-document` and formatted Smart Upload review/summary/conflict date displays via portal Date & Time settings while preserving canonical internal `YYYY-MM-DD` values. | `supabase/functions/extract-document/index.ts`, `src/components/SmartUploadDocument.tsx`, `src/pages/Join.tsx` | Complete. Codex deployed `extract-document`; live invoke of `AA370425004153O_RC07042025.pdf` returned HTTP 200, `is_readable: true`, `detected_type: gst_certificate`, and expected GST fields. Lint/build/Phase 1 readonly smoke PASS on 2026-04-26. |
| COD-ACTIVITIES-COVER-LIST-FALLBACK-015 | Activities list thumbnail effective cover fallback | Codex | Fixed admin/public list cover fallback by normalizing existing gallery-route cover seeds to cover-route seeds, updating list/detail RPCs to return the first gallery image as effective cover when no explicit cover exists, adding `first_media_url` to the admin list RPC, and hardening the admin list `Photo` thumbnail fallback. | `supabase/migrations/20260426090000_activity_effective_cover_fallback.sql`, `supabase/migrations/20260426093000_activity_admin_list_first_media_url.sql`, `src/lib/activityMedia.ts`, `src/lib/supabase.ts`, `src/pages/AdminActivityForm.tsx`, `src/pages/AdminActivities.tsx` | Complete. Migrations applied. Live RPC probe confirmed `cover-card` HTTP 200 image response. Lint/build/Phase 1 readonly smoke PASS on 2026-04-26. |
| COD-ACTIVITIES-SLUG-SEARCH-014 | Activities slug uniqueness + public Events smart search | Codex | Added server-side slug normalization/de-duplication for Activities create/update and implemented ranked smart search with All/Featured/Upcoming/Past filters on public `/events`. Admin slug UI now uses canonical `/events/` prefix and clearer published-slug wording. | `supabase/migrations/20260425150000_activity_slug_uniqueness_and_search.sql`, `src/pages/AdminActivityForm.tsx`, `src/pages/Events.tsx`, `src/lib/supabase.ts` | Complete. Migration applied. Lint/build/Phase 1 readonly smoke PASS on 2026-04-25. |
| COD-ACTIVITIES-MEDIA-AI-FOLLOWUP-013 | Activities gallery batch upload, gallery-cover fallback, and AI date/location extraction options | Codex | Fixed admin Activities gallery multi-select queue so all selected files continue through the crop modal; added save/publish fallback that uses the first active gallery image as cover when no cover is selected; extended source-document extraction to return first-day `activity_date` plus selectable date/location candidates and prefill empty main Activity Details fields. Redeployed `draft-activity-content`. | `src/pages/AdminActivityForm.tsx`, `src/lib/supabase.ts`, `supabase/functions/draft-activity-content/index.ts` | Complete. `supabase functions deploy draft-activity-content`, lint, build, and Phase 1 readonly smoke PASS on 2026-04-25. |
| COD-ACTIVITIES-CLOUDFLARE-MEDIA-012 | Move Activities media originals to private Cloudflare R2 + Worker-served variants | Codex | Implemented private-original Cloudflare media flow for Activities. Migration adds original-object metadata to `activities` / `activity_media`; new Supabase Edge Functions handle upload/delete/original-download; Worker `lub-media` serves display variants from private R2 bucket `lub` on `media.lub.org.in`; admin form supports Cloudflare uploads and `Download Original`. | `src/lib/supabase.ts`, `src/lib/activityMedia.ts`, `src/pages/AdminActivityForm.tsx`, `src/pages/AdminActivities.tsx`, `src/pages/Events.tsx`, `src/pages/ActivityDetail.tsx`, `src/components/ImageCropModal.tsx`, `supabase/functions/activity-media-*`, `supabase/functions/_shared/activity-media-shared.ts`, `supabase/migrations/20260420130000_activities_cloudflare_media_support.sql`, `cloudflare/lub-media/**`, `docs/cloudflare_events_media_setup.md`, `.env.example` | Complete. Migration applied, Worker deployed, functions deployed, live disposable probe passed for cover/gallery upload, Worker display variants, signed original downloads, public no-variant rejection, and cleanup. Lint/build/Phase 1 readonly smoke PASS on 2026-04-25. |
| COD-SMART-UPLOAD-GENDER-PRECEDENCE-011 | Smart Upload review/source cleanup + document-precedence gender override in Join flow | Codex | Codex separated raw Smart Upload review state from draft-only autofill state, added source labels showing whether document data matches or overrides current account/form values, and treated `gender` as a document-precedence field in `/join`. Final browser-verified mismatch case: signup gender `female` plus Aadhaar gender `male` shows `GENDER: Male` with `Document overrides current account/form value`, and continuing to the registration form sets `gender=male`. The override is intentionally scoped to the registration flow and does not mutate the separate account/signup record. | `src/components/SmartUploadDocument.tsx`, `src/pages/Join.tsx` | Complete and browser-verified by Codex on 2026-04-20. |
| CLAUDE-SMART-UPLOAD-GENDER-010 | Fix Aadhaar smart-upload gender handling on Extracted data review + strengthen backend extraction | Claude + Codex | Codex deployed `extract-document`, live-invoked the Aadhaar sample directly, and browser-verified `/join` end to end with a fresh member account. Final root cause was mixed: backend extraction needed the Aadhaar-specific gender hardening that Claude added, and the review card still used draft-only Smart Upload state, which drops successfully extracted fields when they match an existing form value. Codex fixed the remaining client bug by surfacing raw extracted fields from `SmartUploadDocument` into a separate review state in `Join.tsx`, so the review now shows `GENDER: Male` and no longer falsely renders `Unable to extract gender` when extraction succeeded. Final browser verification confirmed: Aadhaar review shows full name, DOB, and gender; the red warning is gone; continuing to the form preserves `gender=male`. | `supabase/functions/extract-document/index.ts`, `src/components/SmartUploadDocument.tsx`, `src/pages/Join.tsx` | Complete and browser-verified by Codex on 2026-04-20. |
| CLAUDE-ADMIN-ACCESS-REDIRECT-009 | Fix admin route infinite redirect loop for authenticated non-admin users | Claude + Codex | `AdminLayout` now distinguishes `unauthenticated` from `authenticated-unauthorized`, sending unauthenticated users to `/signin` and authenticated non-admin users to `/dashboard`. Codex live verification confirmed: admin still reaches `/admin/dashboard`, cleared session goes to `/signin`, and a fresh authenticated `general_user` is redirected from both `/admin/dashboard` and `/admin/members/registrations` to `/dashboard` with no loading hang or loop. | `src/components/admin/AdminLayout.tsx` | Complete and browser-verified by Codex on 2026-04-20. |
| COD-EVENTS-ACTIVITIES-IA-001 | Events/Activities public IA rework + public Events content | Claude + Codex | Claude implementation made `/events` the real public feed, added `/events/:slug`, converted `/activities` and `/activities/:slug` into compatibility redirects, removed public `Activities` nav/footer links, renamed the admin sidebar section to `Events & Activities`, and updated admin `View Public Page` to `/events/:slug`. Codex browser verification confirmed all route, nav, sidebar, and link behavior. | `src/App.tsx`, `src/pages/Events.tsx`, `src/pages/Activities.tsx`, `src/pages/ActivityDetail.tsx`, `src/components/Header.tsx`, `src/components/Footer.tsx`, `src/components/admin/AppSidebar.tsx`, `src/pages/AdminActivities.tsx` | Complete and browser-verified by Codex on 2026-04-20. |
| COD-ACTIVITIES-ACTIONS-MENU-005 | Fix admin Activities row Actions dropdown rendering off-screen | Claude + Codex | Claude correction pass replaced `DropdownMenuTrigger asChild + Button` with a direct trigger using `buttonVariants`, resolving the missing-ref anchor problem. Codex live browser verification confirmed wrapper box `x=1215 y=467 width=176 height=188` and all 5 items render visibly on-screen. | `src/pages/AdminActivities.tsx` | Complete and browser-verified by Codex on 2026-04-19. |
| COD-ACTIVITIES-AI-EXTRACT-BTN-004 | Separate source-document extraction from draft generation with an explicit `Extract Content` button | Claude + Codex | Verified complete. Uploading source documents no longer auto-triggers extraction. The explicit `Extract Content` button appears, extraction runs only when clicked, and guided fields prefill afterward while remaining editable before `Generate Draft`. | `src/pages/AdminActivityForm.tsx` | Complete and browser-verified by Codex on 2026-04-19. |
| COD-ACTIVITIES-HANDOFF-VERIFY-001 | Apply/deploy/runtime-verify Activities AI + list follow-up batches | Codex | Applied `20260420000000`, deployed `draft-activity-content`, and closed the remaining Activities runtime handoff. | `supabase/migrations/20260420000000_activities_limits_default_bump.sql`, `supabase/functions/draft-activity-content/index.ts` | Complete. All runtime checks passed. |
| CLAUDE-ACTIVITIES-NEXT-003 | Activities: source-doc extraction prefill + sort controls + ranked search | Claude | Added AI source-document extraction prefill with 30 MB total cap, sort controls plus explicit `Actions` header, and ranked Activities admin search with Featured-only filtering. | `src/pages/AdminActivities.tsx`, `src/pages/AdminActivityForm.tsx`, `src/lib/supabase.ts`, `supabase/functions/draft-activity-content/index.ts` | Complete. Runtime handoff now closed by `COD-ACTIVITIES-HANDOFF-VERIFY-001`. |
| CLAUDE-ACTIVITIES-FOLLOWUP-002 | Activities AI/provider/media follow-up | Claude | Added honest OpenAI-only AI gating, AI source-document support, structured `error_code` handling, and in-modal gallery ratio selection with portrait options. | `src/pages/AdminActivityForm.tsx`, `src/components/ImageCropModal.tsx`, `src/lib/supabase.ts`, `supabase/functions/draft-activity-content/index.ts` | Complete. Runtime handoff now closed. |
| CLAUDE-ACTIVITIES-BATCH-001 | Activities batch: dropdown fix, limits UI, media UX, AI draft helper, homepage CTA | Claude | Fixed Activities row actions dropdown attempt, added Activities settings UI and higher default gallery cap, improved media handling, added AI draft helper foundation, and changed homepage CTA to `Explore Activities`. | `src/pages/AdminActivities.tsx`, `src/pages/AdminActivityForm.tsx`, `src/pages/AdminActivitySettings.tsx`, `src/pages/Home.tsx`, `src/lib/supabase.ts`, `src/lib/imageProcessing.ts`, `supabase/functions/draft-activity-content/index.ts`, `supabase/migrations/20260420000000_activities_limits_default_bump.sql` | Complete. Live runtime closeout done by Codex. |

